# 系统设计｜道功

## 1. 设计定位

道功是功法体系中负责推动修为增长、承载修炼路线和提供主修资格的功法类型。它回答玩家当前世“以什么路线修炼、能修到哪里、如何吸收与转化灵气、突破读取什么路线标签”的系统问题。

本文只维护道功作为系统对象的定义、学习与研习、主动吐纳、自动运转、掌握度、当前可修上限、自然上限设计和突破路线边界。角色当前选择哪一门道功作为主修、主修更换窗口、锁定记录和突破读取运行时桥接，由《角色主修功法》（`../02_角色与真灵设计/04_角色主修功法.md`）维护；法门能力体系由《法门》（`02_法门.md`）维护；残页、篇章和完整功法载体由《功法载体》（`../05_物品设计/01_功法载体.md`）维护；功法数值模板、掌握曲线和公式接口由《功法设计基准建模》（`../../03_数值设计/05_功法设计基准建模.md`）维护。

## 2. 当前结论

1. 功法是上位泛称，规则层应拆为 `DaoMethod` 道功与 `TechniqueMethod` 法门。
2. 道功是唯一可以推动修为增长并设为主修的功法类型；法门不能设为主修。
3. 道功决定修炼路线、当前可修上限、灵气吸收与转化特质、资源和环境相性、突破路线标签、奖励倾向和失败标签池。
4. 角色通过完整道功载体或明确教学 / 宗门例外入门后创建 `MethodState`；残页和篇章不能直接创建道功掌握状态。
5. 道功掌握度影响当前可修上限、单位时间收益、资源输入兼容、风险提示和可追回知识型收益。
6. 道功掌握度只通过学习、研习或明确的功法系统来源增长；主动吐纳、闭关修炼和自动运转只读取掌握度，不增加掌握度。
7. 道功掌握度不提高理论最高上限，不改变大境界突破模板类型，也不事后改写修为纯度、小境界奖励或阶段性不可追回收益。
8. 可作为主修根基的道功，其自然最高上限原则上落在某个大境界第一小阶段，方便玩家在新大境界改修窗口自然续接。

## 3. 文档边界

| 文档 | 负责内容 |
| --- | --- |
| 本文 | 道功定义、职责、学习研习、吐纳、自动运转、掌握度、上限、突破路线标签和字段建议 |
| `02_法门.md` | 法门定义、能力分类、指令 / 事件 / 效率边界和“不能设为主修”的系统口径 |
| `../02_角色与真灵设计/04_角色主修功法.md` | 主修道功设置、更换窗口、锁定记录、小境界升级约束和大境界突破读取桥接 |
| `../02_角色与真灵设计/03_角色修为与境界.md` | 境界结构、小境界升级、大境界突破入口和当前世修为状态 |
| `../11_修炼玩法设计.md` | 玩家修炼行动、主动吐纳、自动运转、闭关和研习功法入口 |
| `../14_突破玩法设计.md` | 大境界突破准备、B1 突破挑战、资源消耗、失败结果和报告结构 |
| `../05_物品设计/01_功法载体.md` | 残页、篇章、完整功法、合成链、学习来源和载体物品规则 |
| `../../03_数值设计/05_功法设计基准建模.md` | 功法数值模板、掌握度曲线、适配矩阵、道功接入修炼公式和功法结果明细 |
| `../../03_数值设计/02_修炼公式与数值设计.md` | 每小时修炼公式、资源输入、状态压力、`CultivationTickResult` 和调参边界 |
| `../../04_交互设计/05_功法页面设计.md` | 道功列表、详情、研习、主修相关入口、上限提示和普通 UI / Debug 边界 |

## 4. 核心循环或触发入口

道功围绕“获得、研习、入门、主修读取、运转、提升、突破承接”循环维护：

```text
获得完整道功载体或教学授予
→ 选择研习道功
→ 达到入门阈值
→ 创建 MethodState，进入 learned level 1
→ 可按主修规则设为主修道功
→ 主动吐纳 / 自动运转读取道功模板和 MethodState
→ 研习继续提升掌握度
→ 更新当前可修上限、收益、相性和可见路线信息
→ 大境界突破读取道功突破路线标签
```

常见中断点包括载体不完整、目标不是道功、前置知识不足、角色状态禁止研习或运转、神念状态不足、主修道功未设置、当前掌握度不支持目标阶段、当前道功理论上限不足、突破模板缺失和状态压力过高。

## 5. 规则与状态

### 5.1 功法总口径

功法不是单纯技能树，也不是单纯修为倍率表。它在规则层分为两类：

```text
DaoMethod：道功
TechniqueMethod：法门
```

“功法”只作为上位泛称使用，不建议在规则判断中直接用 `method` 表达精确类型。凡涉及修为推进、当前可修上限、大境界突破路线和主修资格的内容，应落到道功；凡涉及战斗指令、行动能力、事件选项和资源效率的内容，应落到法门。

### 5.2 核心定义

道功是可推动修为增长的功法类型，也是唯一可以设为主修的功法类型。

道功决定角色一世内的修行路线根基。玩家选择某门道功，本质是在选择当前大境界的修炼路线，而不只是选择一个收益倍率。

道功路线可以体现为：

1. 快修但修为稳定性或纯度压力较高。
2. 慢修但突破更稳。
3. 依赖特定灵气环境、节点或洞府设施。
4. 擅长转化丹药、灵液、灵材和宗门配给。
5. 偏生存，提供护身、辟谷、解毒、凝神、藏息等被动效果。
6. 偏神魂，强化神念成长、心魔抵抗或神魂攻防。
7. 偏后期，早期效率低但更高阶段上限或路线收益更好。

### 5.3 道功职责

道功负责：

1. 主动吐纳的基础吸收能力。
2. 主动吐纳的基础转化特质。
3. 自动运转的基础收益和稳定性。
4. 当前可修阶段上限。
5. 与灵根、资源输入、节点环境和设施支持的修炼相性。
6. 大境界突破路线标签、挑战倾向、奖励倾向和失败标签池。
7. 主修路线标签。
8. 与该道功绑定的少量被动效果、提示型收益或可追回知识型收益。

道功不负责：

1. 战斗指令的完整展开。
2. 采集、炼丹、炼器、阵法、医术、卜算、隐匿等技艺体系的完整展开。
3. 替代法门系统。
4. 作为物品存在于背包中。
5. 用残页或篇章直接形成运行时学习进度。

边界口径：

| 概念 | 本文是否负责 | 说明 |
| --- | --- | --- |
| 功法 | 只定义总分类口径 | 功法是泛称，规则层应拆为道功与法门 |
| 道功 | 负责 | 修为路线、上限、吸收转化、突破路线标签、被动效果 |
| 主修道功 | 只说明“道功可被设为主修” | 主修状态、切换与锁定由角色主修功法文档负责 |
| 法门 | 不负责 | 法门是能力、指令、事件选项与效率体系 |
| 功法载体 | 不负责 | 载体是物品系统，不是道功掌握状态 |

### 5.4 学习与研习

常规道功流程：

```text
获得完整道功载体
→ 选择研习功法
→ 达到入门阈值
→ 创建 MethodState，进入 learned level 1
→ 可按规则设为主修道功
```

新手基础道功可以由宗门入门事件、新手奖励或教学流程直接授予 learned level 1。该例外用于降低入门摩擦，不代表残页或篇章可以直接学习。

研习道功属于可选投入执行时间的个人持续行动。它读取：

```text
实际投入小时
悟性 aptitude_comprehension
道功阶级 tier_code
道功品级 quality_code
导师
藏经阁、静室、讲经、事件等设施或加成
当前神念状态
前置知识、基础法门或路线理解
```

研习道功可以产出：

1. `MethodState` 经验增加。
2. `MethodState` 掌握等级提升。
3. 未入门道功进入 learned level 1。
4. 道功当前可修上限提高。
5. 主动吐纳或自动运转的单位时间收益提高。
6. 与该道功相关的被动效果、提示型收益或路线信息解锁。

研习道功不能产出：

1. 直接修为进度。
2. 缺失残页或篇章自动补齐。
3. 道功理论最高上限提高。
4. 大境界突破模板类型变化。
5. 小境界升级奖励质量变化。
6. 修为纯度收益变化。
7. 阶段性不可追回收益被事后追回。

### 5.5 主动吐纳与自动运转

主动吐纳是角色主动运转当前主修道功，把环境灵气、设施支持和行动内资源输入转化为修为进度的核心行动。

主动吐纳读取：

1. 当前境界与小阶段。
2. 当前主修道功的吸收速度、转化效率、路线标签和掌握度。
3. 根骨 / 悟性效率、灵根标签匹配、体魄承载、状态压力和伤势。
4. 所在节点灵气、洞府、设施、宗门支持和机会窗口。
5. 行动内选择的丹药、灵液、灵材等资源输入。

主动吐纳输出：

1. 修为进度。
2. 可能的纯度、经脉压力、体内药性消化、残余药性和使用资源日志。

主动吐纳、闭关修炼和自动运转只读取当前主修道功的 `MethodState`、掌握度、吸收、转化和相性，不写入 `MethodState.mastery_exp`、掌握等级或掌握度变化。

自动运转是主修道功最基础的被动表现，用于表达修士日常吐纳不息，但不能替代主动吐纳。

自动运转规则：

1. 按世界小时或兼容 tick 结算低额收益。
2. 主动吐纳期间不额外生效，避免同一 tick 同时获得主动吐纳和自动运转收益。
3. 突破事件链、昏迷、重伤、封印等状态下不生效。
4. 可在合法状态下提供少量修为或稳定性收益。
5. 通常可以触发普通小境界升级。
6. 不能完成大境界突破。
7. 不自动消耗高价值丹药或突破资源。

### 5.6 掌握度与上限

道功掌握度表示角色对该道功的理解、运转熟练度与当前承载能力。

道功有两个上限概念：

```text
theoretical_max_cap_stage：道功理论最高上限
mastery_current_cap_stage：掌握度当前上限
```

当前有效上限：

```text
effective_current_cap_stage = min(
  theoretical_max_cap_stage,
  mastery_current_cap_stage
)
```

道功掌握度可以影响：

1. 当前可修上限。
2. 主动吐纳单位时间收益。
3. 自动运转单位时间收益。
4. 资源输入兼容性、体魄承载稳定性或风险提示。
5. 某些可追回的知识型、提示型或路线型收益。

道功掌握度不能影响：

1. 道功理论最高上限。
2. 修为纯度收益。
3. 小境界升级奖励质量。
4. 大境界突破模板类型。
5. 大境界突破模板自身的核心风险结构。
6. 阶段性不可追回收益。

旧文档中的“功法等级”“道功等级”统一理解为 `MethodState.mastery_level`。它是掌握度层级，不是残页或篇章收集进度。

### 5.7 上限示例

示例：《青木养真诀》

```text
理论最高上限：B-1 100%

level 1：当前可修至 A-2 100%
level 2：当前可修至 A-3 100%
level 3：当前仍可修至 A-3 100%，单位时间收益提高
level 4：当前可修至 B-1 100%
level 5：当前仍可修至 B-1 100%，单位时间收益进一步提高
```

不会发生：

1. level 5 让《青木养真诀》修到 B-2。
2. level 5 让 A-3 → B-1 的突破模板变成另一个模板。
3. level 不足导致 A-1 → A-2 时永久少拿属性。
4. level 不足导致修为纯度永久变差。

### 5.8 与大境界突破的关系

大境界突破模板类型来自道功本身的路线特质，而不是掌握等级临时生成。

可以成立：

```text
《青木养真诀》本身具有 A-3 → B-1 的木系稳健突破转接。
```

不应成立：

```text
《青木养真诀》level 1 匹配低级模板，level 5 匹配另一个高级模板。
```

掌握度影响的是“角色是否承载得住目标阶段、是否满足开启条件、是否获得更稳定的可见度或执行表现”，而不是把一门道功改造成另一种突破模板。

示例：

```text
《青木养真诀》理论最高：B-1
突破转接：A-3 → B-1

level 2 当前只能修到 A-3：
- 可以修到 A-3 100%。
- 不能使用该道功完成 A-3 → B-1。
- 原因是当前掌握度承载不住 B-1。

level 4 当前能修到 B-1：
- 可以使用 A-3 → B-1 的木系稳健突破转接。
- 突破模板类型仍来自道功本身特质，不由 level 4 生成。
```

### 5.9 自然上限设计规范

可作为主修根基的道功，其自然最高上限原则上应落在某个大境界第一小阶段，方便玩家在新大境界的改修窗口内自然续接。

推荐：

```text
A 段道功：A-1 / A-2 / A-3 / B-1
B 段道功：B-1 / B-2 / B-3 / C-1
C 段道功：C-1 / C-2 / C-3 / D-1
```

不推荐：

```text
A-1 / A-2 / A-3 / B-1 / B-2
```

原因是这会把玩家自然卡在 B-2，而 B-2 已离开 B 阶常规改修窗口。

同一传承可以连续，但规则层应拆成多个完整道功模板。

示例：

```text
lineage_id = qingmu_lineage

《青木养真诀》：A-1 → B-1
《青木长生经》：B-1 → C-1
《青木洞玄篇》：C-1 → D-1
```

它们共享传承标签、宗门来源、世界观文本和资源相性，但在规则层是不同完整道功。

## 6. 资源、角色、地图、事件或 NPC 接口

| 接口对象 | 读取 / 写入关系 |
| --- | --- |
| 角色修为状态 | 读取当前境界、小阶段、修为进度和瓶颈状态；道功本身不直接写入境界，只通过修炼或突破结算承接 |
| 主修道功状态 | 读取当前主修道功、`MethodState`、掌握度、当前可修上限、突破路线标签和锁定记录 |
| 角色属性 | 读取根骨、悟性、灵根标签、体魄承载、元气 / 神念和持续状态导出的修正 |
| 持续状态 | 昏迷、重伤、封印、心魔、药性残余、法宝触发态等可以影响吐纳、自动运转、研习或突破前置 |
| 功法载体 | 完整道功载体提供常规学习来源；残页和篇章只提供合成、线索或权限，不创建 `MethodState` |
| 修炼玩法 | 主动吐纳、闭关和自动运转读取道功模板与掌握状态；研习功法写入掌握变化 |
| 突破玩法 | 大境界突破读取道功路线标签、挑战倾向、奖励倾向、失败标签池和掌握度承载情况 |
| 地图节点与设施 | 所在节点灵气、洞府、静室、讲经、藏经阁、宗门支持和设施加成影响道功研习与吐纳 |
| 资源与物品 | 丹药、灵液、灵材、宗门配给等作为修炼资源输入，按道功资源相性和体魄承载结算 |
| 事件与 NPC | 导师、宗门入门、传功、特殊事件或剧情可授予道功、提供研习加成、推荐路线或授权例外 |
| UI 页面 | 展示道功类型、掌握度、当前可修上限、理论最高上限、相性、主修资格、研习入口和不可用原因 |

## 7. 数据字段建议

| 字段 | 含义 |
| --- | --- |
| `DaoMethod.method_id` | 道功模板 ID |
| `method_type` | 功法类型；道功固定为 `dao_method` |
| `lineage_id` | 传承标签，用于同一传承多段道功的世界观与来源归组 |
| `tier_code` / `quality_code` | 道功阶级与品级，用于研习难度、内容层级和数值适配 |
| `route_tags` | 修炼路线标签，用于路线定位、奖励倾向和风险识别 |
| `stage_support_range` | 道功支持的阶段范围 |
| `theoretical_max_cap_stage` | 道功理论最高上限，掌握度不得提高该值 |
| `mastery_curve_id` / `level_cap_profile` | 掌握等级曲线和掌握度当前上限映射 |
| `absorption_profile` / `conversion_profile` | 主动吐纳吸收与转化特质 |
| `root_affinity_tags` / `resource_affinity_tags` / `node_affinity_tags` | 灵根、资源、节点和设施相性入口 |
| `passive_effect_refs` | 道功绑定的少量被动效果、提示型收益或可追回知识型收益 |
| `breakthrough_route_tags` / `breakthrough_support_profile` | 大境界突破路线标签、挑战倾向、奖励倾向和失败标签池入口 |
| `MethodState.method_state_id` | 角色掌握某门道功的运行时状态 ID |
| `MethodState.mastery_level` / `mastery_exp` | 掌握等级与掌握经验 |
| `MethodState.mastery_current_cap_stage` | 由掌握度决定的当前可修上限 |
| `MethodState.unlocked_effect_refs` / `active_tags` | 已解锁效果、路线信息或当前生效标签 |

道功模板建议字段：

```yaml
DaoMethod:
  method_id: string
  method_type: dao_method
  display_name: string
  lineage_id: string
  tier_code: string
  quality_code: string
  route_tags: list
  stage_support_range: list
  theoretical_max_cap_stage: string
  mastery_curve_id: string
  level_cap_profile: map
  absorption_profile: map
  conversion_profile: map
  root_affinity_tags: list
  resource_affinity_tags: list
  node_affinity_tags: list
  passive_effect_refs: list
  breakthrough_route_tags: list
  breakthrough_support_profile: map
  compatibility_requirements: map
```

角色对某门道功的掌握状态由 `MethodState` 表达。`MethodState` 不保存残页或篇章收集进度。

```yaml
MethodState:
  method_state_id: string
  character_id: string
  method_id: string
  method_type: dao_method
  learned_from_carrier_id: string
  mastery_level: number
  mastery_exp: number
  mastery_current_cap_stage: string
  unlocked_effect_refs: list
  active_tags: list
  last_practiced_world_day: number
  last_practiced_world_hour: number
```

字段建议是实现提示，不等同于最终数据库或代码结构。

## 8. UI 展示边界

普通 UI 应展示：

1. 道功名称、类型、传承、阶级、品级和当前是否已学会。
2. 当前掌握等级、掌握进度、当前可修上限和理论最高上限。
3. 是否可设为主修，以及不可设为主修的原因。
4. 道功路线标签、资源 / 灵根 / 节点相性摘要和适合的修炼场景。
5. 主动吐纳、自动运转或研习道功时对玩家可理解的收益、风险和短板提示。
6. 与突破相关的可见摘要，例如路线倾向、承载不足、状态压力和准备短板。

普通 UI 不应展示：

1. 完整失败标签池、奖励配置引用、原始倍率、公式缓存或 Debug 追踪。
2. 残页 / 篇章收集进度作为道功掌握等级。
3. 掌握度直接改变突破模板类型的暗示。
4. 道功理论最高上限之外的越界续修承诺。
5. 法门能力被误展示为主修道功效果。

## 9. MVP 范围

MVP 应实现：

1. `DaoMethod` 与 `TechniqueMethod` 的类型区分。
2. 道功可推动修为增长、可设为主修；法门不能设为主修。
3. 完整道功载体或明确新手 / 宗门例外创建 `MethodState`。
4. 道功研习提升 `MethodState.mastery_level`、`mastery_exp` 和当前可修上限。
5. 主动吐纳读取当前主修道功的吸收、转化、相性、掌握度和状态压力，但不提升掌握度。
6. 自动运转提供低额收益并受状态和主修规则限制。
7. `theoretical_max_cap_stage`、`mastery_current_cap_stage` 和 `effective_current_cap_stage` 的上限约束。
8. 大境界突破读取道功本身的突破路线标签，而不是由掌握等级临时生成模板。
9. 道功模板与 `MethodState` 字段的最小数据口径。
10. 功法页面展示道功掌握、上限、相性、主修资格和不可用原因。

MVP 不实现：

1. 残页或篇章直接学习道功。
2. 掌握度提高道功理论最高上限。
3. 掌握度改变大境界突破模板类型。
4. 掌握度事后追回修为纯度、小境界升级奖励或阶段性不可追回收益。
5. 功法推演、功法创造、功法融合、废功重修或改修折算。
6. 道功替代法门的战斗、采集、炼丹、炼器、阵法、医术、卜算和隐匿体系。
7. 自动运转完成大境界突破或自动消耗高价值丹药 / 突破资源。

## 10. 验收约束

1. 任何可以推动正常修为增长并设为主修的功法必须是道功。
2. 法门不得写入主修道功字段，不得决定当前可修上限或大境界突破模板类型。
3. 残页和篇章不得创建 `MethodState`，不得直接提高道功掌握度。
4. 道功模板不得保存角色个人掌握进度；角色进度必须落在 `MethodState`。
5. `effective_current_cap_stage` 必须同时受理论最高上限和掌握度当前上限约束。
6. 掌握度不得提高 `theoretical_max_cap_stage`。
7. 掌握度不得把一门道功的大境界突破模板改造成另一种模板。
8. 道功理论最高上限之外不得通过自动运转、小境界升级或资源输入继续推进。
9. 主动吐纳期间不得额外叠加自动运转收益。
10. 自动运转不得完成大境界突破，也不得自动消耗高价值丹药或突破资源。
11. 主动吐纳、闭关修炼和自动运转不得写入道功掌握经验、掌握等级或掌握度变化。
12. 可作为主修根基的道功若自然卡点不在大境界第一小阶段，需要进入待裁决或特殊设计说明。
13. UI 不得把残页 / 篇章进度、法门能力或 Debug 字段表达成道功掌握状态。

## 11. 待裁决与待补

| 项目 | 当前口径 | 需要裁决或补充 |
| --- | --- | --- |
| 道功模板表 | 已列出 `DaoMethod` 建议字段 | 需要补齐正式模板表、字段枚举和示例数据 |
| 道功吸收速度表 | 由 `absorption_profile` 表达 | 需要数值文档补齐不同阶级、品级和路线的曲线 |
| 道功转化效率表 | 由 `conversion_profile` 表达 | 需要确认与根骨、灵根、资源输入和体魄承载的组合方式 |
| 道功与灵根相性表 | 通过 `root_affinity_tags` 进入匹配 | 需要确认灵根标签全集、倍率口径和无匹配时是否只有无增幅 |
| 资源、节点和设施适配矩阵 | 使用 `resource_affinity_tags`、`node_affinity_tags` 和设施支持入口 | 需要补齐丹药、灵液、灵材、洞府、静室、讲经等适配矩阵 |
| 道功被动效果表 | 只允许少量被动、提示型收益或可追回知识型收益 | 需要确认哪些被动进入 MVP，哪些交给法门或持续状态系统 |
| 突破路线标签与结果配置 | 由 `breakthrough_route_tags` 和 `breakthrough_support_profile` 表达 | 需要补齐挑战倾向、奖励倾向、失败标签池和突破文档引用格式 |
| 自然上限例外 | 原则上落在大境界第一小阶段 | 需要裁决是否允许特殊道功自然卡在非第一小阶段，以及提示和换修处理 |

## 12. 包内关联文档

| 文档 | 关系 |
| --- | --- |
| `02_法门.md` | 并列唯一信源：维护法门定义、能力边界和不能设为主修的规则 |
| `../02_角色与真灵设计/04_角色主修功法.md` | 承接：读取本文道功定义、掌握度、上限和突破路线，维护主修运行时规则 |
| `../02_角色与真灵设计/03_角色修为与境界.md` | 承接：提供境界结构、小境界升级和大境界突破状态口径 |
| `../11_修炼玩法设计.md` | 承接：主动吐纳、自动运转、闭关和研习行动读取道功规则 |
| `../14_突破玩法设计.md` | 承接：大境界突破读取道功突破路线标签和承载条件 |
| `../05_物品设计/01_功法载体.md` | 承接：提供完整道功载体、残页、篇章和学习来源边界 |
| `../../03_数值设计/05_功法设计基准建模.md` | 被承接：维护道功数值模板、掌握度曲线、适配矩阵和功法结果明细 |
| `../../03_数值设计/02_修炼公式与数值设计.md` | 被承接：维护主动吐纳、资源输入和修炼 tick 的公式结算 |
| `../../04_交互设计/05_功法页面设计.md` | 展示：表达道功掌握、上限、相性、主修资格、研习入口和不可用原因 |

## 13. 维护记录

2026-05-12：按系统设计模板整理本文，标题改为“系统设计｜道功”；补齐设计定位、当前结论、文档边界、核心循环、资源接口、数据字段建议、UI 展示边界、MVP、验收约束、待裁决、包内关联和维护记录。保留原有道功总口径、学习研习、主动吐纳、自动运转、掌握度、上限示例、大境界突破关系、自然上限规范和字段建议。

2026-05-21：根据修炼系统数值投放收敛裁决，明确主动吐纳、闭关修炼和自动运转只读取道功掌握度，不增加 `MethodState` 掌握经验或掌握等级；道功掌握增长由研习功法等功法系统来源承接。
