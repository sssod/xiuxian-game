# 物品设计｜资源与材料

## 1. 当前结论

MVP 不需要让所有资源都是具体物品实例。推荐层级：

- 宗门大宗资源可先抽象为库存数值。
- 节点自然产能可先作为 ResourcePool / ResourceSlot。
- 普通材料、丹药、符箓可作为可堆叠物品。
- 法宝、功法载体、正式后手物、高价值突破资源、带独立状态的物品应作为实例物品。

正式后手物指已封存进真灵藏宝的实例物品。它们可由同一真灵跨世感应和开启，但必须受真灵藏宝设置数量和单个容量限制。宗门个人洞府中的材料或资源可按宗门遗产清点回收；宗门外洞府和普通藏宝中的材料属于无主遗留设施资产，需满足开启条件或破阵结果后才能取出。

## 2. 宗门抽象资源

MVP 宗门追踪五类资源：

1. 灵石。
2. 修炼资源。
3. 材料 / 矿材。
4. 人力 / 弟子规模。
5. 声望 / 影响力。

声望 / 影响力不是货币，不直接兑换物品，而是行动修正、外部响应、控制权竞争和事件承担参数。人力不是物品，可被占用、损耗、恢复或转化为执行完成度修正。

## 3. 自然资源池与加工转化

自然资源点首先管理资源状态，不直接等价于背包物品或可领取库存。节点自然产能可用 `ResourcePool` 或 `ResourceSlot` 表达；只有经过采集、开采、维护、加工、炼制、宗门持续行动、事件结算或资源槽附属设施产出后，才转化为库存资产、抽象宗门资源、可堆叠材料或独立实例物品。

资源池最小字段：

```yaml
NaturalResourcePool:
  resource_pool_id: string
  node_ref: string
  resource_slot_ref: string | null
  resource_type: aura | ore | herb | beast | ecology | space | special
  tier_code: string
  abundance_state: rich | normal | poor | depleted
  current_amount: number
  max_amount: number
  regeneration_rate: number
  extraction_difficulty: number
  depletion_risk: number
  pollution_state: none | minor | serious | corrupted
  discovery_state: hidden | rumored | discovered | controlled
  controlling_faction_ref: string | null
  controlling_faction_bonus: number
  attached_facility_ref: string | null
  ledger_refs: list
```

约束：

1. 自然资源不消耗世界物品预算帽，受 `max_amount`、`regeneration_rate`、`depletion_risk`、污染状态、资源槽规模、控制权和事件状态约束。
2. 资源池可由事件、宗门建设、秘境变化、灾害、污染、迁移、清理或世界演化创建、削弱、枯竭或销毁。
3. 宗门经营和玩家持续采集必须读取同一资源池状态，避免同一资源被重复结算。
4. 宗门已控制资源点后的基础采矿、药田收成、灵脉基础抽取、初级材料加工和已建成设施稳定产出，可作为常态结算或持续项目，不必都占用宗门主行动。
5. 新资源点开采、深层开采、过度开采、资源异变处理、抢占 / 恢复资源点、修复损坏设施、建设高级加工设施、尝试产出特殊物品或高阶材料，仍可占用宗门持续行动或触发事件链。
6. 资源点产出进入库存前分两类转化：简单加工规则用于普通材料，配方生产规则用于特定物品、特殊材料或更高品级结果。

## 4. 配方生产

```yaml
Recipe:
  recipe_id: string
  display_key: string
  recipe_category: alchemy | forging | talisman | array | refining | repair | copy | processing
  tier_code: string
  quality_floor: string
  quality_ceiling: string
  required_method_tags: list
  required_recipe_level: number
  required_facility_tags: list
  input_requirements: list
  optional_inputs: list
  output_prototype_refs: list
  output_stack_policy: stackable | unique_instance
  base_batch_size: number
  time_cost: object
  resource_cost: object
  failure_modes: list
  allowed_random_fields: list
  variance_profile: object
  byproduct_rules: object
  recycle_rules: object
  upgrade_rules: object
  modification_slots: list
```

配方是“明确投入 -> 明确产出”的生产规则，不是随机掉落表。它服务于丹药、符箓、阵盘、法宝修复、材料加工、功法拓印、灵材精炼、建筑构件、宗门常态加工、NPC 工坊、坊市服务和特殊事件制造。

配方来源可以是非战斗类功法 / 技艺、NPC 传授、玉简 / 手札 / 残方 / 传承印记、宗门职位与藏经阁权限、任务奖励、秘境发现或正式后手回收。功法 / 技艺提供理解、效率、品质上限、失败控制和改造能力；配方提供具体产出规则。配方不等于功法本身。

配方生产请求：

```yaml
RecipeProductionRequest:
  request_id: string
  actor_type: character | sect | npc_workshop
  actor_ref: string
  recipe_ref: string
  source_container_ref: string
  input_resources: list
  optional_inputs: list
  facility_ref: string
  operator_method_tags: list
  operator_skill_level: number
  recipe_level: number
  batch_count: number
  time_budget: object
  quality_intent: stable | efficient | high_quality | risky
  risk_acceptance: object
  rng_namespace: string
  log_policy: object
```

生产结果：

```yaml
RecipeProductionResult:
  success: bool
  produced_stacks: list
  produced_instances: list
  byproducts: list
  consumed_inputs: list
  returned_inputs: list
  recipe_exp_delta: number
  item_quality_result: object
  failure_reason: string | null
  item_logs: list
  stock_logs: list
```

随机边界：

1. 核心产物类型由配方决定，不由随机决定。
2. 投入资源满足最低条件时，至少产生可用结果或明确失败返还。
3. 随机主要影响品级、数量副产、状态桶、成色桶、副词条、失败副作用和返还比例。
4. 高阶配方可以有更大波动，但必须提前展示风险。
5. 批量生产优先生成 `ItemStack`，不为每个单体创建独立实例。
6. 产出价值不得长期稳定高于投入价值，除非由稀有配方、稀有设施、高阶技艺、长时间成本、高失败风险、关键材料稀缺、宗门权限或地域限制解释。

配方养成可作为接口保留：

```yaml
RecipeProgression:
  recipe_id: string
  known_by_actor: bool
  recipe_level: number
  mastery_exp: number
  quality_floor_bonus: number
  variance_reduction: number
  input_efficiency_bonus: number
  batch_size_bonus: number
  byproduct_chance_bonus: number
  failure_recovery_bonus: number
  modification_unlocks: list
```

配方改造是高级玩法，MVP 不要求完整展开，但字段应允许替换核心材料、改变副词条倾向、降低副作用、提高批量产出、增加突破 / 战斗 / 修复 / 封存等用途标签，或将稳定配方改造成高风险高收益配方。

## 5. 价值、预算与回收口径

资源与材料至少区分四类价值：

| 价值 | 用途 |
| --- | --- |
| `TradeValue` | 买卖、估价、商店价格、市场挂牌 |
| `BudgetValue` | 事件、秘境、掉落、宝藏等生成预算扣除 |
| `SalvageValue` | 拆解、修复折价、残片产出和商店回收 |
| `ContributionValue` | 上交宗门后的贡献、声望、职位评价或权限修正 |

规则：

1. `BudgetValue` 不必等于 `TradeValue`。
2. `SalvageValue` 通常低于交易价值，受物品状态、设施、角色技艺、污染和物品类型影响。
3. `ContributionValue` 可读取 `TradeValue` 并乘以宗门需求程度，但贡献不是货币。
4. 出售、拆解、销毁、上交、回收或消耗不返还世界生成预算。
5. 正式后手封存物的身份来自既有显性物品记录，不重新生成；状态变化按日志修正。

物品标准价值可用下式作为建模起点：

```text
V_std = B_category * T_tier * Q_quality * E_effect * S_scarcity * C_condition * U_utility
```

字段含义：

| 字段 | 含义 |
| --- | --- |
| `B_category` | 物品类型基础价值 |
| `T_tier` | 阶级倍率 |
| `Q_quality` | 品级倍率 |
| `E_effect` | 效果预算倍率 |
| `S_scarcity` | 稀缺度倍率 |
| `C_condition` | 状态、耐久、完整度、充能、污染等倍率 |
| `U_utility` | 用途覆盖倍率 |

MVP 调参起点：

```text
T_tier = R_tier ^ (tier_order - 1)
R_tier = 8 ~ 20

Q1 下品 = 0.70
Q2 中品 = 0.90
Q3 上品 = 1.15
Q4 极品 = 1.45
Q5 绝品 = 1.90
```

这组数值只是平衡起点，不是定稿。品级差异应明显，但不应超过跨阶差异。

状态倍率示例：

```text
耐久完整 = 1.00
轻微受损 = 0.80
严重受损 = 0.45
破碎但可修复 = 0.20
污染且未净化 = 0.30 ~ 0.70
```

用途越多，价值越高：

```text
U_utility = 1 + utility_bonus - restriction_penalty
```

用途包括战斗、突破、修炼、宗门上交、制造 / 修复、拆解、封存和任务提交。限制包括绑定角色、宗门权限、使用阶段、地点、一次性消耗、副作用或因果风险。

世界预算价值不使用通货价格：

```text
V_final = V_std * D_random * D_optional_origin * D_information
V_world_budget = V_final
```

约束：

1. `V_world_budget` 不乘通胀系数。
2. `V_world_budget` 不受市场买卖价影响。
3. 跨阶使用时，生成预算仍按物品自身阶级扣除。
4. 交易价格可另行读取通胀、供需、关系、节点状态、权限、声望和事件压力修正。

交易价格可保留接口：

```text
BuyPrice = V_final * InflationCoeff * market_buy_spread * relation_modifier * scarcity_modifier
SellPrice = V_final * InflationCoeff * market_sell_spread * relation_modifier * liquidity_modifier
```

突破资源不只按直接数值估价，还要读取降低失败风险或提高突破质量的能力：

```text
V_breakthrough = V_std * (1 + risk_reduction_value + success_quality_value + stage_specificity_value)
```

配方产出不消耗世界预算帽，但必须受投入约束：

```text
OutputValueExpected <= InputValue * recipe_efficiency * skill_modifier * facility_modifier * risk_modifier
```

## 6. MVP 验证闭环与最小模板

MVP 至少验证以下资源与材料闭环：

1. 资源点到宗门库存：宗门控制资源节点 -> 资源池按再生规则增加 -> 宗门常态经营采集 / 加工 -> 生成抽象宗门资源或材料 `ItemStack` -> 写入宗门库存日志。
2. 玩家持续采集：玩家在节点持续采集 / 加工 -> 读取资源池当前状态 -> 消耗行动时间与补给 -> 按技艺、工具、风险和事件结果转为材料 `ItemStack` -> 写入角色日志与节点资源变化。
3. 配方生产：玩家、宗门或 NPC 工坊掌握配方 -> 投入明确资源、材料、设施和时间 -> 产出明确目标物品 -> 品级、数量或状态只在配方范围内随机 -> 写入配方经验、资产流水和结果包。
4. 探索 / 事件生成：玩家探索节点、隐藏层或秘境 -> 事件按难度、地点和资源倾向生成候选物 -> 检查对应阶级生成预算 -> 玩家获得后扣预算并进入显性库存或事件锁定库存。
5. 战斗隐性实例化：战斗中按 NPC 或事件模板实例化法宝、符箓、丹药或材料；战后若被获得，进入显性库存并按来源扣预算；若未获得，回收为隐性、损坏或销毁。
6. 突破资源：玩家申请宗门资源、自行收集或配方生产突破资源 -> 突破系统读取资源支持维度 -> 突破消耗、损坏或释放资源 -> 成功 / 失败写日志和结果包。
7. 正式后手：玩家将已有显性物品封存为真灵藏宝 -> 写入后手记录和资产流水 -> 转世后同一真灵感应和开启 -> 取出时原物品重新进入合法容器。
8. 通货与高密度资源：普通灵石可通过加工转化为聚灵石；高阶消耗不直接无限消耗普通灵石；加工设施效率形成普通灵石通胀与高阶需求之间的阀门。

最小资源点模板：

```text
resource_pool_A_mine
resource_pool_A_herb_field
resource_pool_A_spirit_site
```

每个资源点至少配置当前资源量、资源上限、再生速度、采集难度、枯竭 / 污染状态和控制宗门修正。

最小配方模板：

```text
recipe_basic_healing_pill
recipe_basic_talisman
recipe_basic_material_processing
recipe_basic_artifact_repair
recipe_basic_spirit_stone_processing
```

每个配方至少配置输入资源、输出物品、品级范围、失败结果、堆叠策略和配方经验。

最小世界生成池：

```text
pool_newbie_support
pool_basic_sect_supply
pool_A_exploration_reward
pool_A_combat_reward
pool_A_breakthrough_resource
pool_A_hidden_treasure
pool_A_npc_trade_implicit_stock
```

正式后手不进入世界生成池，单独配置回收状态：

```text
legacy_recovery_state_basic
legacy_recovery_state_exposed
legacy_recovery_state_damaged
legacy_recovery_state_moved
```

最小预算帽配置：

```yaml
WorldItemBudgetConfig:
  tier_code: string
  none_reached_rate: number
  some_reached_rate: number
  all_reached_rate: number
  max_accumulated_value: number
```

MVP 调试界面应能看到当前各阶预算累积值、本宏观周期增长值、本宏观周期消耗值，以及因预算不足而降级或替换的生成结果。

MVP 最小随机字段：

1. 品级随机。
2. 数量随机。
3. 状态随机：完整、轻微受损、严重受损。
4. 一个关键副词条或缺陷。
5. 识别 / 封印状态：已识别、未识别、封印未解。
6. 堆叠策略：可堆叠或独立实例。
7. 随机桶：纯度桶、状态桶、效果波动桶。
8. 可选来源字段：仅在法宝、功法载体、命名秘宝、宗门信物、正式后手封存物、任务关键物等模板中启用。

MVP 不建议立刻支持大量复合词条、复杂套装、完整市场价格波动、多阶段制造链，或因微小随机差异导致普通道具无法堆叠。

最小经济参数：

```yaml
EconomicBaseline:
  reference_spirit_stone_value: number
  min_inflation_coeff: number
  max_inflation_coeff: number
  inflation_smoothing_rate: number
  sink_cost_multiplier_rules: object
  spirit_stone_to_low_currency_exchange_rule: object
```

## 7. 缺口

- 材料 / 矿材分类表。
- 修炼资源与具体丹药、灵材、配给之间的转换规则。
- 配方表、设施需求、品质随机规则。
- 自然资源池的资源类型枚举、再生周期、枯竭 / 污染事件表和采集难度表。
- 物品基础价值表、阶级倍率、品级倍率、状态倍率和突破资源估值表。
- 世界生成池、预算帽数值、随机桶表和调试展示粒度。

## 8. 包内依赖与边界

本页的解释前提限于当前重整包。资源与材料的当前口径以本文正文为准；需要展开的底层规则只依赖当前重整包内文档。

包内承接关系：

1. 资产容器、库存类型、可堆叠物品、独立实例、预留、锁定、释放和资产流水，见《../06_储物空间系统设计/03_库存系统.md》。
2. 宗门五类抽象资源、宗门库存、资源申请、资源预留、宗门持续行动消耗和审计，见《../17_宗门系统设计.md》。
3. 地图节点、资源槽、资源槽附属设施、节点常态产出和产出入库，见《../03_经济系统设计/01_地图资源产出.md》。
4. 世界阶段预算、事件投放和非节点来源的资源生成边界，见《../03_经济系统设计/02_世界凭空产出.md》。
5. 物品来源、资产位置、转移、消耗、拆解、回收、价值口径和资产可信链，见《../03_经济系统设计/03_物品流转.md》。
6. 投放预算、资源槽抽样、事件生成预算、宗门库存、市场交易和世界窗口的数值边界，见《../../03_数值设计/07_投放数值设计.md》。
7. 灵石、低阶货币、聚灵石、灵石加工和高密度灵气资源输入，见《05_灵石及聚灵石.md》。
8. 丹药、符箓、法宝、功法载体等具体物品类型的身份、堆叠和实例化规则，见《02_丹药.md》《04_符箓.md》《03_法宝.md》《01_功法载体.md》。
9. 行动资源输入、残余效果和修炼资源使用交互，见《../11_修炼玩法设计.md》；每小时资源输入数值、资源效果定价和结算公式，见《../../03_数值设计/02_修炼公式与数值设计.md》。
10. 个人洞府库存、普通藏宝、真灵藏宝、正式后手物和无主遗留设施资产边界，见《../18_个人洞府库存_藏宝与后手系统设计.md》。

上述包内文档未补齐的内容，保留为本文“缺口”，不得以隐含规则补齐。若本文与包内专题文档出现冲突，优先按负责该主题的包内专题文档处理；无法机械判断的设计取舍应返回用户裁决。
