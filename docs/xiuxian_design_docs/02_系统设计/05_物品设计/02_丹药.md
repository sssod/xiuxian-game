# 系统设计｜丹药

## 1. 设计定位

丹药是可追踪资产，也是行动、事件选项、显式炼化或自动吸收过程中的资源输入。本文负责维护丹药在系统层的身份、使用入口、消耗时机、残余药性、炼丹生产边界、日志与玩家决策约束。

丹药不作为装备栏式被动挂件，也不作为绕过世界时间的即时收益按钮。修为丹药只能作为主动吐纳资源输入；非修为丹药可按配置进入行动资源输入、显式炼化、自动吸收或事件专用结算。

丹药的公式、倍率、首批样例数值和预算不在本文维护，应由《修炼公式与数值设计》（`../../03_数值设计/02_修炼公式与数值设计.md`）、《物品设计基准建模》（`../../03_数值设计/04_物品设计基准建模.md`）和后续数值表承接。丹药页面表达、资源输入折叠项和提示样式由交互文档承接。

## 2. 当前结论

1. 丹药、灵材、符箓、临时增益资源统一通过资源使用接口和日志进入结算；行动型丹药通过 `ActionResourceInputBinding` 绑定到具体行动或事件选项。
2. 丹药效果只在兼容行动、兼容事件、兼容危险语境、显式炼化过程或自动吸收过程中按实际小时结算。
3. 行动开始、事件选项生效、炼化指令开始或自动吸收确认前再次校验资源；未开始或未确认则不消耗。
4. 行动、炼化或自动吸收已开始但资源未完全用完时，生成资源残余类持续状态；本文中的 `ActiveResourceEffect` 是该状态的实现提示名。
5. 修为丹药不提高节点灵气或聚灵阵倍率，而是提供独立定额灵气包，并进入后续体魄承载、根骨转化、相性和状态压力结算。
6. 巩固根基可以选择性收束残余修为药性，但不能把残余药性转化为修为进度。
7. 突破丹药由突破事件链专门结算，不进入普通残余药性状态。
8. 所有消耗、残余、衰减、退款、失败原因和结果都进入资产日志、结果包和回放。

## 3. 文档边界

| 文档 | 负责内容 |
| --- | --- |
| `../09_行动指令设计.md` | 行动队列、资源输入绑定、执行前校验、局部时停和 C1 追赶 |
| `../11_修炼玩法设计.md` | 主动吐纳、巩固根基、修为丹药作为修炼资源输入的玩法入口 |
| `../14_突破玩法设计.md` | 突破准备、突破关键资源、突破事件链和突破丹药专用结算 |
| `../02_角色与真灵设计/07_角色持续状态系统.md` | 资源残余状态、自动吸收状态、叠加互斥、生命周期和状态导出 |
| `../03_经济系统设计/03_物品流转.md` | 丹药资产容器、来源、预留、消耗、退款和资产流水 |
| 本文 | 丹药系统规则、使用方式、消耗时机、残余药性、炼丹生产边界和验收约束 |
| `../../03_数值设计/02_修炼公式与数值设计.md` | 修为丹药进入修炼结算后的公式、转化、压力和验算 |
| `../../03_数值设计/04_物品设计基准建模.md` | 丹药阶级、品级、预算、资源效果模板和价值基准 |
| `../../04_交互设计/04_修为页面设计.md` | 主动吐纳、巩固根基和修为药性提示的页面表达 |
| `../../04_交互设计/07_储物空间页面设计.md` | 丹药库存、来源容器、使用入口、消耗确认和资产状态展示 |

## 4. 核心循环或触发入口

丹药围绕“获得、存放、绑定、校验、消耗、按小时结算、残余处理、日志回放”循环维护。

```text
获得丹药
-> 进入角色随身、储藏、宗门库存、商店、NPC、节点资源库、事件锁定奖励或后手封存容器
-> 玩家在行动、事件选项、显式炼化或自动吸收入口选择丹药
-> 创建资源使用请求或行动资源绑定
-> 执行前再次校验持有、权限、语境、叠加和安全条件
-> 开始后消耗丹药并按实际兼容小时释放效果
-> 未释放部分进入资源残余类持续状态、散逸、暂停、退款或事件专用处理
-> 写入资产日志、角色日志、结果包和回放
```

常见触发入口：

1. 主动吐纳附加修为丹药。
2. 研习功法附加悟性丹。
3. 毒瘴移动、探索、采集、战斗或事件中使用避瘴 / 解毒丹。
4. 休整、治疗、医疗事件或战后救治中使用疗伤 / 恢复丹。
5. 巩固根基、突破准备、突破事件链或研习功法中使用定心 / 清神丹。
6. 属性丹、洗髓丹、延寿丹、高价值悟性丹或强稳定类丹药在安全环境中显式炼化。
7. 常规恢复、回气、解毒或止血丹药使用后自动吸收。
8. 炼丹生产行动产出丹药模板或实例。

中断点包括资源不在合法容器、权限不足、行动或事件标签不兼容、环境不安全、同组残余药性未处理、角色状态禁止吸收、丹药已被占用或消耗、事件模板不允许丹药输入、炼丹行动被中断等。

## 5. 规则与状态

### 5.1 物品身份

普通丹药可以作为可堆叠物品；高价值、命名、突破关键、延寿、剧情危险、后手封存或带独立状态的丹药应作为 `ItemInstance` 追踪。

最小字段建议：

```text
PillItem {
  item_instance_id: String
  item_template_id: String
  container_id: String
  quantity: int
  tier: String
  quality: String
  tags: Array
  resource_effect_template_id: String
  source_ref: String
  visibility_policy: String
  bound_effect_refs: Array
}
```

堆叠条件至少要求模板、阶级、品级、关键状态相同，且未绑定独立事件、后手、来源审计或特殊缺陷。阶级表达丹药适配的境界大层级；品级表达同阶内部的纯度、稳定性、药力成色和副作用风险。

丹药必须位于某个资产容器，例如角色随身、角色储藏、宗门库存、商店库存、NPC 持有物、节点资源库、事件锁定奖励或后手封存。宗门库存中的丹药可以成为行动资源输入来源，但必须经过权限、申请、预留、消耗和审计日志。

### 5.2 丹药分类与语境

| 类型 | 对应行动 / 语境 | 效果通道 | 使用方式 / 计时模式 |
| --- | --- | --- | --- |
| 修为丹药 | 主动吐纳 | `cultivation_aura` | `action_resource_input` / `compatible_action_hours` |
| 悟性丹 | 研习功法；少数高价值悟性丹可安全炼化 | `method_insight` | `action_resource_input` 或 `explicit_refinement` |
| 属性丹 / 洗髓丹 | 安全环境炼化 | `attribute_modifier` / `aptitude_modifier` | `explicit_refinement` |
| 解毒丹 / 避瘴丹 | 毒瘴移动、探索、采集、战斗；普通解毒可自动吸收 | `hazard_resist` | `hazard_exposure_hours` 或 `auto_refinement` |
| 疗伤丹 / 恢复丹 | 休整、治疗、医疗事件、战后救治；普通恢复可自动吸收 | `injury_recovery` / `recovery_over_time` | `compatible_action_hours`、事件模板指定或 `auto_refinement` |
| 定心丹 / 清神丹 | 巩固根基、突破准备、突破事件链、研习功法 | `pressure_control` / `breakthrough_modifier` | `compatible_action_hours` 或事件模板指定 |
| 战斗丹药 | 战斗准备、战斗事件链指令、轮内选项 | 战斗模板指定 | 事件模板指定 |
| 突破关键丹药 | 突破准备、开启突破、突破挑战轮内资源 | `breakthrough_modifier` | 突破模板指定 |
| 延寿丹 / 寿元丹药 | 完成炼化后直接增加寿元上限 | `lifespan_extension` | `explicit_refinement` / 完成炼化结算 |
| 危险剧情丹药 | 剧情传功、强行破关、邪道速成、濒死续命 | `forced_release` 分支 | MVP 完全不投放，仅未来预留 |

突破丹药和普通丹药必须区分。普通丹药可按资源模板进入资源残余状态；所有突破丹药都由突破事件链专门结算，不进入普通残余药性状态。突破事件结束后，突破丹药停止影响，只写入突破结果包、资源消耗日志和角色状态变化日志。

### 5.3 资源效果模板

丹药效果应由 `ResourceEffectBalance` 或等价模板定价，不写成固定 raw 修为奖励。

```text
ResourceEffectBalance {
  resource_effect_template_id: String
  effect_type: cultivation_aura | method_insight | pressure_control | injury_recovery | recovery_over_time | hazard_resist | breakthrough_modifier | attribute_modifier | aptitude_modifier | lifespan_extension
  use_mode: action_resource_input | explicit_refinement | auto_refinement | event_only
  valid_segment_tags: Array
  compatible_action_tags: Array
  compatible_context_tags: Array
  effect_tick_mode: compatible_action_hours | hazard_exposure_hours | world_elapsed_hours | refinement_completion
  unit_effect_rate: number
  effect_rate_min: number
  effect_rate_max: number
  effect_random_distribution: fixed | normal_clamped
  max_effective_hours: number
  refinement_duration_hours: number | null
  safe_environment_required: bool
  auto_absorb_without_extra_command: bool
  benchmark_hours_saved: number
  normalized_progress_bonus: number
  contribution_cap_ratio: number
  quality_scaling_profile: Dictionary
  stimulation_level: low | medium | high
  stability_level: stable | volatile
  residual_policy: stable_suspend | slow_decay | stimulating_decay | forced_release
  can_be_settled_by_consolidation: bool
  settlement_policy: none | suppress_side_effect | release_without_progress
  stacking_group: String
  max_same_group_active: int
}
```

资源价值需要通过当前境界段、丹药阶级、体魄承载、功法相性、灵根 / 药性标签、状态压力和贡献上限校验。低阶丹药可以安全但贡献降低；高阶丹药可以潜在更强，但 MVP 中主要表现为浪费、体魄承载不足、短期反噬或经脉压力；不适配标签丹药主要表现为转化率低、药力浪费、短期疲劳、短期经脉压力或短期心神扰动。

丹药允许主效果波动，但波动必须直接写在资源效果模板中。若 `effect_rate_min` 与 `effect_rate_max` 相同，该丹药按确定性效果结算；若二者不同，则在该范围内按截断正态分布抽取实际效果。实际抽取值应在资源绑定、效果实例、结果包和回放日志中固化；C1 追赶和回放只使用已经固化的抽取值，不重新随机。

### 5.4 行动绑定与消耗时机

行动规划、炼化指令或事件选择创建资源绑定：

```text
ActionResourceInputBinding {
  binding_id: String
  command_id: String | null
  event_option_id: String | null
  character_id: String
  source_container_id: String
  resource_item_ref: String
  resource_item_template_id: String
  resource_effect_template_id: String
  effect_type: String
  use_mode: action_resource_input | explicit_refinement | auto_refinement | event_only
  compatible_action_tags: Array
  compatible_context_tags: Array
  effect_tick_mode: String
  unit_effect_rate: number
  max_effective_hours: number
  activation_policy: consume_on_action_start | consume_on_event_option | consume_on_refinement_start | consume_on_use_confirm
  refinement_duration_hours: number | null
  safe_environment_required: bool
  residual_policy: String
  residual_stability_hours: number
  stacking_group: String
  max_same_group_active: int
  created_world_day: int
  created_world_hour: int
  consumed_at_world_day: int | null
  consumed_at_world_hour: int | null
  active_effect_ref: String | null
}
```

入队时只校验当前拥有和当前可用；队列不锁定未来资源，也不因为前序炼丹、采购或宗门申请可能成功而允许后续直接使用丹药。

执行前必须再次校验角色持有或调用权限、合法资产容器、行动或事件语境、行动标签、地点 / 危险 / 事件上下文、安全环境、自动吸收条件、叠加限制和同组未处理残余。

行动未开始、事件选项未生效、炼化指令未开始或使用确认未完成时，不消耗关键丹药。行动、炼化或自动吸收被事件打断时，已生效部分正常结算，未生效部分根据模板进入残余效果、散逸、退款、暂停或事件专用处理。

### 5.5 按小时结算

执行过程中按小时结算：

```text
compatible_hours = 本片段中资源可生效的小时数
applied_hours = min(compatible_hours, effect.remaining_effective_hours)
applied_amount = effect.unit_effect_rate * applied_hours
```

不同效果通道进入不同管线：

```text
修为丹药：applied_amount -> resource_aura_input
悟性丹药：applied_amount / bonus -> method_exp_gain 修正
解毒丹药：applied_amount -> toxic_risk_delta / poison_pressure_delta
疗伤丹药：applied_amount -> vitality_recovery / injury_delta
定心丹药：applied_amount -> mind_stability_delta / risk_tag_delta
突破丹药：applied_amount -> breakthrough_success_delta / breakthrough_quality_delta / risk_delta
延寿丹药：完成炼化后 -> LifespanState.lifespan_limit / lifespan_extension_sources
```

### 5.6 修为丹药

修为丹药只能作为主动吐纳资源输入。

合法：

```text
主动吐纳 24 小时，附加清灵丹 x 1。
```

非法：

```text
即时服用清灵丹，立即获得修为。
单独执行丹药炼化 12 小时，并获得修为。
移动途中服用清灵丹，持续获得修为。
```

主动吐纳中存在两条输入流：

| 输入流 | 规则 |
| --- | --- |
| 环境灵气流 | 节点灵气、资源槽灵气、聚灵阵、洞府、灵液池等，受道功吸收速度限制 |
| 修为丹药资源流 | 丹药释放定额灵气包，跳过道功吸收速度，但仍进入根骨转化、体魄承载、状态压力和标签匹配结算 |

修为丹药不是提高节点灵气浓度、不是提高聚灵阵效果，也不是简单提高最终倍率。

合流公式：

```text
cultivation_resource_aura_input =
  sum(unit_aura_rate_i * applied_effective_hours_i * resource_quality_factor_i)

raw_aura_input =
  environment_aura_input + cultivation_resource_aura_input
```

之后统一进入体魄安全承载、根骨转化效率、灵根与道功相性、道功基础转化率、状态修正、修为进度和修炼压力。

主动吐纳实际执行时长短于丹药 `max_effective_hours` 时，只消耗实际兼容小时，剩余药性进入资源残余状态。

示例：

```text
行动：主动吐纳 6 小时
资源：清灵丹 x 1
清灵丹：20 灵气 / 小时，最多 12 小时

本次释放灵气：20 * 6 = 120
剩余有效时长：6 小时
剩余可释放灵气：120
```

### 5.7 非修为丹药炼化与自动吸收

非修为丹药可以支持“丹药炼化”，但必须通过配置限制使用方式、环境、安全性和消耗时间。

| 使用方式 | 适用对象 | 规则 |
| --- | --- | --- |
| `action_resource_input` | 悟性丹、定心丹、避瘴丹、疗伤丹等行动内资源 | 绑定具体行动或事件选项，在兼容小时内生效 |
| `explicit_refinement` | 属性丹、洗髓丹、高价值悟性丹、延寿丹、强稳定类丹药 | 创建炼化指令，要求安全环境，按配置消耗 `refinement_duration_hours` |
| `auto_refinement` | 常规疗伤、回气、解毒、止血、恢复类药物 | 使用后创建自动吸收效果，不占额外炼化行动，但按世界小时或兼容恢复小时逐步生效 |

增加属性、洗髓、延寿、长期稳定或其他长期改变角色状态的丹药，默认需要安全环境炼化。安全环境由配置判定，可包括宗门静室、洞府、医馆、闭关室、受保护节点、非战斗状态、非移动状态、无高危环境暴露等条件。

常规恢复类丹药可以在使用后自动炼化吸收，不需要额外创建长期炼化行动。自动吸收仍然不是即时生效；它创建持续效果或残余效果，按配置的单位时间回复量结算。自动吸收期间若角色进入战斗、移动、危险环境或状态冲突，是否继续生效、暂停、加速消耗或转为风险，由模板配置。

延寿丹 / 寿元丹药规则：

1. MVP 实现延寿丹，效果为完成显式炼化后直接增加 `LifespanState.lifespan_limit`。
2. 延寿丹不在使用确认时即时完整结算；未完成炼化前不提前增加寿元。
3. 丹药延寿不在丹药层面设置独立总量上限。寿元模块统一限制丹药来源寿元总量，始终按当前非丹药总寿元的百分比钳制，例如不超过当前非丹药总寿元的 20%。
4. 超过寿元模块上限的丹药延寿部分不生效，但消耗、失败原因、实际生效寿元和被上限钳制的寿元都必须写入结果包与角色日志。
5. 延寿丹的具体数值、炼化时长、安全环境、效果是否波动和品级倍率仍由丹药模板配置。

### 5.8 残余药性

资源残余状态表示丹药已被消耗，但效果未在本次行动中完全用完。它属于《角色持续状态系统》（`../02_角色与真灵设计/07_角色持续状态系统.md`）中的资源残余类状态；丹药文档只定义创建条件、资源模板和消耗口径，状态生命周期、叠加互斥、失效和属性修正导出由角色持续状态系统统一管理。

生成场景：

1. 行动实际时长少于丹药最大有效时长。
2. 行动开始后被事件打断。
3. 行动中部分小时不满足丹药兼容语境。
4. 显式炼化未完成。
5. 自动吸收尚未释放完整效果。

不生成场景：

1. 带资源输入的行动没有实际开始。
2. 服务端校验未通过，丹药没有消耗。
3. 事件模板声明该资源为一次性无残余。
4. 所有突破丹药由突破事件链专门结算，突破事件结束后停止影响。
5. 丹药模板声明使用失败时全额退款且不产生状态。

最小结构：

```text
ActiveResourceEffect {
  effect_id: String
  character_id: String
  source_binding_id: String
  resource_effect_template_id: String
  effect_type: String
  unit_effect_rate: number
  remaining_effective_hours: number
  remaining_effect_amount: number
  compatible_action_tags: Array
  compatible_context_tags: Array
  effect_tick_mode: compatible_action_hours | hazard_exposure_hours | world_elapsed_hours
  residual_policy: stable_suspend | slow_decay | stimulating_decay | forced_release
  residual_stability_hours_remaining: number
  idle_world_hours: number
  can_be_settled_by_consolidation: bool
  settlement_policy: none | suppress_side_effect | release_without_progress
  stacking_group: String
  source_command_id: String | null
  source_event_option_id: String | null
  created_world_day: int
  created_world_hour: int
  last_applied_world_hour: int
  state: active | suspended | decaying | exhausted
  log_refs: Array
}
```

残余策略：

| 策略 | 用途 | 规则 |
| --- | --- | --- |
| `stable_suspend` | 普通清灵丹、普通启悟丹、低刺激养元资源 | 非兼容行动期间不生效，在稳定窗口内不明显损失，之后缓慢散逸 |
| `slow_decay` | 品质较差丹药、保存状态一般资源 | 非兼容行动期间缓慢减少，通常只造成轻微浪费或疲劳 |
| `stimulating_decay` | 速成修为丹、烈性火系丹药、高刺激破关资源 | 非兼容行动期间较快衰减，衰减可能转为经脉压力、疲劳或神念扰动 |
| `forced_release` | 高阶危险丹药、剧情丹药、强行破关资源 | 按世界小时持续释放，未被吸收部分转为浪费、压力或反噬；MVP 完全不投放，只预留字段 |

残余效果允许跨行动、跨世界小时保存，并随角色存档。决策阶段、局部时停和玩家阅读报告期间不推进药性衰减。C1 追赶只补结算确定性的兼容小时、资源消耗和药性衰减；若丹药模板有随机效果，追赶只读取已固化的实际效果值，不触发新的随机事件。

### 5.9 巩固根基与残余修为药性

巩固根基可以选择性收束残余修为药性，但不转化为修为进度。它的作用是止损、减压、稳定根基、降低散逸或短期副作用风险。

稳定残余药性默认不自动收束，必须由玩家明确选择。刺激型或强制释放型药性应强提示风险，并建议继续主动吐纳或巩固根基收束。

收束结算：

```text
settled_hours = min(action_actual_hours, effect.remaining_effective_hours)
settled_amount = effect.unit_effect_rate * settled_hours
```

`settled_amount` 不进入 `resource_aura_input` 或 `cultivation_progress_gain`，而进入 `pressure_prevention`、`side_effect_reduction` 和 `residual_effect_clearance`。如果巩固根基本身有纯度收益或根基收益，该收益来自巩固行动本身，不来自修为丹药残余。

### 5.10 事件选项中的丹药

事件选项允许临时追加丹药输入，但必须由事件模板声明 `resource_input_slot`。

示例：

```text
毒瘴战斗事件中临时服用避瘴丹。
突破交锋中临时服用定心丹。
战斗中临时服用回气丹。
医疗事件中临时使用疗伤丹。
```

事件丹药规则：

1. 不允许回溯生效。
2. 必须走统一资源校验。
3. 必须写入 `ResourceUseRequest`、`ResourceUseResult`、事件结果包、资产日志和角色日志。
4. 是否产生残余效果由事件模板和丹药模板共同决定。
5. 突破事件链中的丹药优先服从突破事件链资源规则，不产生普通残余药性，突破事件结束后停止影响。

禁止：

```text
事件发生后，回溯让避瘴丹覆盖之前已经暴露的 5 小时。
战斗结束后，回溯使用回气丹改变已结算的交锋。
突破失败后，回溯服用定心丹重算风险。
```

### 5.11 叠加与托管

MVP 同一行动中，同一效果通道最多使用 1 个主资源输入。

示例：

```text
主动吐纳：最多 1 种主修为丹药。
研习功法：最多 1 种主悟性增益资源。
毒瘴探索：最多 1 种主避瘴 / 解毒资源。
休整治疗：最多 1 种主疗伤资源。
```

如果角色已有同一 `stacking_group` 的资源残余状态，默认不允许再使用同组新丹药。玩家应先通过兼容行动承接、等待散逸，或通过巩固根基收束。

显式托管不得主动消耗新的稀有修为丹药、高刺激速成丹药或不可逆高价值资源。托管可以在安全条件下处理已有残余效果，例如继续主动吐纳承接稳定药性，或巩固根基收束刺激型药性。保命、避险、避免重大不可逆选择优先于节省药性。

### 5.12 炼丹生产边界

丹药生产可以使用轻量配方制，但不做完整炼丹炼器工业链。

轻量配方制要求：

1. 消耗明确材料、灵石、宗门资源、设施时段或特殊药引。
2. 产出明确丹药模板。
3. 随机性只允许发生在配方声明的范围内，例如品级、数量副产、成色、缺陷、失败副作用或返还比例。
4. 产出价值不得长期稳定高于投入价值，除非由稀有配方、稀有设施、高阶技艺、长时间成本、高失败风险、关键材料稀缺、宗门权限或地域限制解释。
5. 丹药生产消耗和产出必须进入资产流水、结果包和回放。

炼丹生产是固定执行时间行动，可以是长期低交互行动并参与 F1；但队列不会因为前序炼丹可能成功而允许后序使用尚未产出的丹药。

MVP 中，炼丹生产行动一旦开始后被中断，直接判定本炉炼制失败。未开始前仍不消耗材料；已开始后，已投入材料、费用和设施时段不返还，不暂停、不按进度折算，也不产出完成丹药。失败结果包只记录失败原因、消耗和日志。

### 5.13 已裁决口径

| 冲突 | 当前裁决 |
| --- | --- |
| 丹药是独立“丹药炼化”行动，还是行动资源输入 | 修为丹药只能作为主动吐纳资源输入；非修为丹药可按配置显式炼化或自动吸收 |
| 修为丹药是否即时增加修为 | 不允许；必须依附主动吐纳实际兼容小时 |
| 非修为丹药是否可以炼化 | 可以；属性丹等长期改变类丹药需要安全环境和配置时长，常规恢复类丹药可使用后自动吸收 |
| 修为丹药是否提高节点灵气或聚灵阵倍率 | 不提高；它提供独立定额灵气包，并进入后续根骨转化、体魄承载和状态压力 |
| 决策阶段是否推进药性衰减 | 不推进；只有世界小时实际结算时推进 |
| 巩固根基是否能把残余药性转为修为 | 不能；只能止损、减压、稳定和清理残余 |
| 突破丹药是否产生普通残余药性 | 不产生；所有突破丹药由突破事件链结算，突破事件结束后停止影响 |
| 延寿丹 / 寿元丹药是否进入 MVP | 进入；完成显式炼化后直接增加寿元上限，丹药延寿总量由寿元模块按当前非丹药总寿元百分比限制 |
| 常规 MVP 是否投放强制释放丹药 | 不投放；MVP 完全不投放危险丹药，`forced_release` 只预留字段和未来分支 |
| MVP 是否引入长期丹毒 / 杂质累计值 | 不引入；只保留短期压力、疲劳、心神扰动和未来扩展接口 |
| 炼丹生产中断后的损耗规则 | MVP 中炼丹行动开始后被中断，直接判定炼制失败；未开始则不消耗 |
| 丹药使用效果是否允许随机波动 | 允许；随机波动写在模板效果上下限中，按截断正态分布抽取，非随机丹药把上下限配置为同一数值 |

## 6. 资源、角色、地图、事件或 NPC 接口

| 接口对象 | 读取 / 写入关系 |
| --- | --- |
| 行动系统 | 读取行动标签、执行时长、局部时停、打断和 C1 追赶；写入资源绑定、消耗时点和结果包 |
| 修炼系统 | 读取主动吐纳、巩固根基、道功相性和修炼压力；写入修为资源输入或残余收束结果 |
| 突破系统 | 读取突破准备、事件链阶段和突破资源槽；写入突破丹药消耗、成功率 / 风险修正和结果包 |
| 角色持续状态系统 | 写入资源残余、自动吸收、恢复、压力或短期副作用状态；读取叠加互斥和生命周期 |
| 角色属性与寿元 | 写入属性、资质、恢复、寿元上限和短期状态变化；寿元模块负责统一限制丹药延寿总量 |
| 物品流转与库存 | 读取丹药容器、权限、预留、堆叠、实例和来源；写入消耗、退款、转移、失败和审计日志 |
| 地图、节点与事件 | 读取地点安全性、危险语境、资源槽、事件选项和资源输入槽；写入事件使用结果 |
| NPC 与宗门 | 读取交易、发放、申请、配给、借用或夺取权限；写入宗门库存审计和 NPC 资产变化 |
| 炼丹生产 | 读取配方、材料、设施、宗门权限和时间成本；写入丹药产出、失败、损耗和回放 |
| UI 与回放 | 读取丹药兼容性、预计释放小时、残余风险、消耗原因和日志摘要；不读取 Debug 原始倍率作为普通展示 |

## 7. 数据字段建议

| 字段 | 含义 |
| --- | --- |
| `PillItem` | 丹药物品或实例提示结构，记录模板、容器、数量、阶级、品级、来源和绑定效果 |
| `ResourceEffectBalance` | 丹药效果模板，记录效果类型、使用方式、兼容标签、单位效果、随机范围、残余策略和叠加组 |
| `ActionResourceInputBinding` | 行动、事件、炼化或自动吸收入口创建的资源绑定 |
| `ActiveResourceEffect` | 丹药已消耗但未完全释放时生成的资源残余状态提示名 |
| `resource_effect_template_id` | 指向丹药资源效果模板 |
| `effect_type` | 丹药效果通道，例如修为灵气、悟性、压力控制、恢复、避险、属性、寿元 |
| `use_mode` | 使用方式：行动资源输入、显式炼化、自动吸收或事件专用 |
| `effect_tick_mode` | 结算时钟：兼容行动小时、危险暴露小时、世界小时或炼化完成 |
| `unit_effect_rate` | 单位小时效果，数值含义由效果通道解释 |
| `effect_rate_min` / `effect_rate_max` | 主效果波动区间；相同则为确定性效果 |
| `max_effective_hours` | 最大可生效小时 |
| `refinement_duration_hours` | 显式炼化需要的世界小时 |
| `safe_environment_required` | 是否要求安全环境 |
| `auto_absorb_without_extra_command` | 是否使用后自动吸收且不额外占用炼化行动 |
| `residual_policy` | 残余药性策略：稳定暂停、缓慢散逸、刺激散逸或强制释放 |
| `can_be_settled_by_consolidation` | 是否可由巩固根基收束 |
| `settlement_policy` | 收束后是否抑制副作用、清除压力或释放但不转进度 |
| `stacking_group` / `max_same_group_active` | 同类丹药叠加组和上限 |
| `activation_policy` | 消耗触发时机：行动开始、事件选项生效、炼化开始或使用确认 |
| `source_container_id` | 丹药来源容器 |
| `source_command_id` / `source_event_option_id` | 残余状态来源，用于日志、回放和追溯 |
| `lifespan_extension_sources` | 延寿丹完成炼化后写入寿元模块的来源记录 |

字段建议是实现提示，不等同于最终数据库或代码结构。

## 8. UI 展示边界

资源输入 UI 采用行动内折叠项。玩家选择具体行动后，只显示与该行动兼容的丹药和资源输入。

主动吐纳界面应显示：

```text
可投入丹药
来源容器
单位效果
最大有效小时
预计本次释放小时
预计残余小时
相性提示
经脉压力或副作用风险
```

已有残余效果必须在角色面板或行动规划器中常驻提示。

稳定残余提示：

```text
体内仍有未尽药性：清灵丹，约 6 小时。
执行主动吐纳可继续承接。
若长时间不处理，药性可能散逸。
```

刺激型残余提示：

```text
体内仍有烈性药性：赤焰速元丹，约 4 小时。
非修炼行动期间可能快速散逸并造成经脉压力。
建议：继续主动吐纳或巩固根基收束。
```

事件选项中的丹药作为附加条件或子选项显示，必须明确消耗什么丹药、是否立即消耗、是否产生残余效果、是否会中断 / 改变 / 接续当前行动、可能降低什么风险、仍然保留什么风险，以及若为炼化或自动吸收需要多少世界小时释放完整效果。

普通 UI 不展示：

1. 内部模板 ID、绑定 ID、日志 ID、资源流水 ID 或 Debug 预算字段。
2. 未揭示丹药的完整隐藏缺陷、剧情真相或强制释放分支。
3. 可被误解为“即时服丹涨修为”的按钮或文案。
4. 未经事件模板允许的回溯用药入口。
5. 原始公式追踪、随机种子或未固化抽取过程。

## 9. MVP 范围

MVP 应实现：

1. 丹药通过统一资源使用接口进入结算；行动型丹药作为 `ActionResourceInputBinding`。
2. 修为丹药挂载主动吐纳。
3. 悟性丹挂载研习功法。
4. 避瘴 / 解毒丹挂载危险语境行动或事件。
5. 疗伤丹挂载休整、治疗或医疗事件。
6. 定心丹挂载巩固根基、突破准备、研习功法或突破事件链。
7. 延寿丹完成显式炼化后直接增加寿元上限，并由寿元模块统一限制丹药延寿总量。
8. 行动未开始不消耗资源。
9. 行动开始后按实际兼容小时消耗资源。
10. 资源残余状态和自动吸收状态。
11. `stable_suspend`、`slow_decay`、`stimulating_decay` 三类常规残余策略。
12. `forced_release` 字段预留，但不投放危险丹药内容。
13. 巩固根基可选择性收束残余修为药性。
14. 事件选项可临时追加丹药输入。
15. 同效果通道最多 1 个主丹药输入。
16. 资源使用日志、资产流水、结果包和回放。
17. 行动内折叠式资源输入 UI。
18. 非修为丹药的 `use_mode`、炼化时长、安全环境和自动吸收字段。
19. 丹药效果模板按 `effect_rate_min` / `effect_rate_max` 配置是否波动，并固化实际抽取值。
20. 轻量炼丹生产行动，开始后中断则本炉失败，未开始不消耗材料。

MVP 不实现：

1. 通过独立“丹药炼化”让修为丹药增加修为。
2. 即时服丹涨修为。
3. 多枚修为丹药复杂叠加。
4. 主药 + 辅药 + 药引组合。
5. 危险丹药和 `forced_release` 实际投放内容。
6. 可长期累计的复杂丹毒 / 杂质系统。
7. 丹药作为装备栏式被动挂件。
8. 玩家在结算阶段主动暂停重新安排药性。
9. 完整炼丹炼器工业链。

## 10. 验收约束

1. 修为丹药不得通过即时服用或独立丹药炼化直接增加修为。
2. 丹药确认使用前必须再次校验资源、容器、权限、语境、行动标签、安全环境和叠加限制。
3. 行动、事件、炼化或自动吸收未开始时，不得消耗关键丹药。
4. 已开始后必须按实际兼容小时结算，不得默认结算完整效果。
5. 未释放完的丹药效果必须进入残余、散逸、暂停、退款或事件专用处理，不得静默丢失。
6. 残余药性属于角色持续状态系统统一管理，丹药文档不得单独定义冲突的生命周期。
7. 决策阶段、局部时停和玩家阅读报告期间不得推进药性衰减。
8. C1 追赶不得重新随机丹药效果，只能读取已固化的抽取值。
9. 突破丹药不得进入普通残余药性状态，必须由突破事件链结算。
10. 巩固根基收束残余修为药性时，不得把该药性转为修为进度。
11. 延寿丹未完成显式炼化前不得提前增加寿元；丹药延寿总量由寿元模块统一钳制。
12. 同一行动中同一效果通道最多使用 1 个主丹药输入，除非未来文档显式扩展叠加规则。
13. 炼丹生产已开始后被中断，MVP 直接判定本炉失败；消耗和失败原因必须进入结果包。
14. 所有丹药消耗、失败、退款、残余、衰减和收益必须进入资产日志、角色日志、结果包和回放。

## 11. 待裁决与待补

| 项目 | 当前口径 | 需要裁决或补充 |
| --- | --- | --- |
| 丹药阶级、品级与境界段适配 | 使用阶级、品级、相性、贡献上限和压力风险字段，但不在本文写死数值 | 是否采用固定五档品级显示，以及每档对药力、稳定性、残余策略和副作用的倍率 |
| 资源模板样例数值 | 本文定义字段和结算口径 | 清灵丹、启悟丹、避瘴丹、疗伤丹、定心丹等首批样例数值、兼容小时和残余策略 |
| 主药 + 辅药 + 药引组合 | MVP 不做多资源复杂叠加 | 正式版是否开放丹方组合，以及叠加上限、体魄承载压力和 UI 复杂度边界 |
| 延寿丹具体模板 | MVP 保留延寿丹并交由寿元模块统一限制总量 | 首批延寿丹的炼化时长、安全环境、品级倍率、波动范围和稀有度 |
| 自动吸收中断策略 | 由丹药模板配置继续、生效、暂停、加速消耗或转为风险 | 首批恢复 / 解毒 / 回气类丹药在战斗、移动和危险环境中的默认处理 |
| 炼丹轻量配方 | MVP 采用轻量配方制，不做完整工业链 | 首批配方、材料、设施、失败副作用、副产和品级随机范围 |
| 危险丹药未来分支 | MVP 不投放，`forced_release` 只作字段预留 | 非 MVP 是否启用剧情危险丹药，以及强制释放、反噬和提示边界 |

## 12. 包内关联文档

| 文档 | 关系 |
| --- | --- |
| `../09_行动指令设计.md` | 承接：行动队列、资源输入校验、局部时停和 C1 追赶 |
| `../11_修炼玩法设计.md` | 承接：主动吐纳、巩固根基和残余药性玩法提示 |
| `../14_突破玩法设计.md` | 承接：突破准备、突破关键资源和突破事件链 |
| `../02_角色与真灵设计/07_角色持续状态系统.md` | 被承接：丹药药性、自动吸收、恢复和残余状态进入统一持续状态管理 |
| `../02_角色与真灵设计/02_角色属性.md` | 承接：属性、资质、寿元和派生状态的角色层边界 |
| `../03_经济系统设计/03_物品流转.md` | 承接：资产容器、来源、流转、消耗、退款和日志 |
| `../06_储物空间系统设计/03_库存系统.md` | 承接：个人、宗门、节点、商店等库存读取 |
| `../../03_数值设计/02_修炼公式与数值设计.md` | 承接：修为丹药进入主动吐纳后的公式、压力和验算 |
| `../../03_数值设计/04_物品设计基准建模.md` | 承接：丹药阶级、品级、预算、资源效果模板和物品价值 |
| `../../03_数值设计/07_投放数值设计.md` | 承接：丹药投放、宗门资源、节点机会和世界预算 |
| `../../04_交互设计/04_修为页面设计.md` | 展示：主动吐纳资源输入、残余修为药性和巩固根基提示 |
| `../../04_交互设计/07_储物空间页面设计.md` | 展示：丹药库存、来源容器、使用入口和资产状态 |

## 13. 维护记录

2026-05-12：按系统设计模板整理本文，标题改为“系统设计｜丹药”；补齐当前结论、文档边界、核心循环、资源接口、数据字段建议、UI 展示边界、验收约束、待裁决与待补、包内关联文档和维护记录；保留原有丹药身份、分类、资源效果模板、行动绑定、修为丹药结算、非修为丹药炼化、残余药性、巩固根基、事件丹药、叠加托管、炼丹生产、MVP 范围和已裁决冲突口径。
