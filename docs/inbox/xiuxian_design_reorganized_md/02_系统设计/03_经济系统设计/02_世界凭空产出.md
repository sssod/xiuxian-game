# 经济系统设计｜世界凭空产出

## 1. 当前结论

“凭空产出”不等于无约束刷物品。事件、宝藏、NPC 掉落、秘境奖励等隐性或实例化内容，应受分阶生成预算和来源语境约束。

## 2. 生成预算规则

经济系统需要同时维护：

1. 库存流转：已经显性存在的资源和物品在容器之间移动，底层见《06_储物空间系统设计/03_库存系统》。
2. 生成预算：事件、宝藏、NPC 掉落、秘境奖励等“凭空生成”内容受分阶预算帽约束。

规则：

- 一旦物品由事件 / 宝藏 / 隐性状态实例化，就按物品完整预算价值扣除生成预算。
- 后续出售、拆解、损坏、销毁、上交、封存，不返还生成预算。
- 生成预算用于控制世界投放，不是玩家可见资源，也不是资产回收池。
- 若生成结果暂不可领取，应先进入事件锁定库存，不应直接伪装成行囊物品。

## 3. 资产生成请求

```yaml
AssetGenerationRequest:
  source_system: string
  generation_context: object
  tier_budget_ref: string
  reward_profile_ref: string
  target_container_ref: string | null
  event_locked_first: bool
  visibility: string

AssetGenerationResult:
  success: bool
  generated_assets: list
  budget_value_spent: number
  target_container_ref: string
  ledger_entry: string
  failure_reason: string | null
```

## 4. 后手与遗产

正式后手不是凭空生成，而是显性物品储存、封存、跨世追踪。MVP 阶段正式后手以真灵藏宝表达，由同一真灵跨世感应和开启，且受设置数量和单个容量限制。

普通遗产也不是生成预算返还。玩家角色转世前留下的宗门个人洞府库存，按 NPC 死亡后的宗门相关遗产规则清点并回收宗门；宗门外个人洞府、普通藏宝和真灵藏宝不按遗产回收，不会因为转世自动进入宗门库存或新世行囊。

宗门外个人洞府和普通藏宝需要满足钥匙、功法、线索、位置、事件条件或破阵结果后才能进入 / 取出，可能被其他角色提前打开。这类开启只移动已有资产，不凭空生成物品。

## 5. 缺口

- 各阶预算点数未定。
- RewardProfile、宝藏、秘境、NPC 掉落模板未定。
- 预算消耗与多人可见性的具体联动未定。

## 6. 关联文档

1. 库存、事件锁定资产和资产流水见《06_储物空间系统设计/03_库存系统》。
2. NPC 掉落与持久化资产边界见《16_NPC设计》。
3. 藏宝、真灵藏宝和普通遗产见《18_个人洞府库存_藏宝与后手系统设计》。
