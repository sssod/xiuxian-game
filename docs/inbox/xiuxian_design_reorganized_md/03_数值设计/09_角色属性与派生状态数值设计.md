# 数值设计｜角色属性与派生状态

## 1. 设计定位

本文负责角色属性相关的数值层设计，包括单位、输入参数、公式、乘区、配置表、保留值约束、派生状态上限、寿元派生、丹药延寿上限、缓存字段和调参验收口径。

本文不负责角色属性字段语义、来源 / 持续性口径、灵根系统边界、寿元系统职责、UI 展示原则或旧字段迁移。这些系统层规则由《角色属性》（`../02_系统设计/02_角色与真灵设计/02_角色属性.md`）维护。伤势、药性、符箓持续效果、法宝触发态和事件持续状态的生命周期由《角色持续状态系统》（`../02_系统设计/02_角色与真灵设计/07_角色持续状态系统.md`）维护，本文只读取其导出的后天修正合计和派生状态结算结果。

## 2. 当前结论

1. 角色属性系统文档是字段语义和模块边界主文档；本文是数值层唯一配套文档。
2. 常规数值公式优先采用“绝对值先合并、百分比同乘区加减汇总”的单乘区结构。
3. 年龄阶段是体魄 / 精力最终值计算中的独立乘区，不并入生机、元气或神念的普通增幅系数。
4. 生机最大值读取体魄最终值，元气最大值读取精力最终值，神念最大值不读取体魄、精力、年龄阶段或悟性资质。
5. 非灵根资质有效值不读取年龄阶段；灵根在 MVP 只读取先天结果。
6. 生机、元气、神念的保留值会减少可支配上限；当前值不得超过扣除保留值后的可用容量。
7. 丹药延寿进入 MVP，但有效总量由寿元模块按当前非丹药总寿元比例限制。
8. 缓存字段不是事实来源；事实来源仍是角色属性、持续状态记录、资源效果、功法效果、事件结果和数值表。

## 3. 文档边界

| 文档 | 负责内容 |
| --- | --- |
| `../02_系统设计/02_角色与真灵设计/02_角色属性.md` | 角色属性字段语义、系统规则、来源 / 持续性口径、属性边界、寿元职责、UI 展示原则和旧字段迁移 |
| `../02_系统设计/02_角色与真灵设计/07_角色持续状态系统.md` | 持续状态生命周期、叠加互斥、后天修正导出和状态结算输出 |
| 本文 | 单位、公式、表结构、倍率、预算、缓存字段、调参边界和数值验收 |
| `08_数值验算.md` | 验算样例、曲线检查和异常测试 |
| `../04_交互设计/03_角色页面设计.md` | 展示属性、派生状态、寿元压力、揭示状态和数值摘要 |

## 4. 基准、单位与输入参数

### 4.1 单位口径

| 项目 | 口径 |
| --- | --- |
| 属性值 | 使用数值点数，具体范围由配置表决定 |
| 派生状态 | 使用当前值、最大值、保留值三类数值 |
| 百分比字段 | 统一使用 `*_percent` 命名；文档中的 `100%` 可由实现层保存为百分数或等价倍率 |
| 绝对值加成 | 先按来源合并为 `*_flat_bonus` 或同类字段，再进入公式 |
| 年龄 | 使用世界时间推进后的当前世年龄，单位与寿元上限一致 |
| 寿元 | 使用当前世最大时间预算；寿元上限变化后，年龄比例和寿元压力重新派生 |

### 4.2 输入参数

| 参数 | 来源 | 说明 |
| --- | --- | --- |
| `age` | `LifespanState` | 当前年龄 |
| `lifespan_limit` | `LifespanState` | 当前寿元上限 |
| `base_physique.innate_value` | `BaseAttributes` | 体魄先天值 |
| `base_physique.acquired_permanent_delta` | `BaseAttributes` | 体魄后天永久修正合计 |
| `base_physique.temporary_modifiers_total` | 持续状态系统与后天修正接口 | 当前有效体魄临时修正合计 |
| `base_vigor.innate_value` | `BaseAttributes` | 精力先天值 |
| `base_vigor.acquired_permanent_delta` | `BaseAttributes` | 精力后天永久修正合计 |
| `base_vigor.temporary_modifiers_total` | 持续状态系统与后天修正接口 | 当前有效精力临时修正合计 |
| `aptitude_bone` / `aptitude_comprehension` | `AptitudeProfile` | 根骨 / 悟性先天、后天永久和临时修正 |
| `aptitude_root` / `root_affinity_profile` | `AptitudeProfile` | MVP 只读取先天灵根结果 |
| `*_aging_phase_multiplier` | 年龄阶段数值表 | 体魄 / 精力年龄阶段乘区 |
| `*_coeff_percent` | 派生状态系数表 | 体魄 / 精力到生机 / 元气的换算系数 |
| `*_flat_bonus` | 状态、资源、功法、事件等 | 派生状态绝对值加成合计 |
| `*_amplifier_percent` / `mind_fixed_multiplier_percent` | 状态、资源、功法、事件等 | 派生状态百分比增幅合计 |
| `non_pill_lifespan_limit` | 寿元来源汇总 | 当前非丹药来源寿元总量 |
| `pill_lifespan_extension_total_raw` | 丹药延寿来源汇总 | 丹药来源原始延寿总量 |
| `pill_lifespan_extension_cap_ratio` | 寿元配置 | 丹药延寿有效总量上限比例 |

## 5. 公式或模型

### 5.1 通用公式口径

常规数值公式优先采用单乘区结构：

```text
result =
  (base_value +/- flat_delta_total)
  * (100% +/- percent_delta_total)
```

规则：

1. 同一公式里的多个绝对值来源先加减合并，再进入乘区。
2. 同一百分比乘区内的多个增减幅来源用加减法汇总，不互相独立连乘。
3. 默认无增减幅时，百分比乘区为 `100%`。
4. 不默认设置多个独立乘区，除非有明确设计理由。
5. 年龄阶段是体魄 / 精力最终值计算中的例外独立乘区。

### 5.2 寿元派生与年龄阶段

```text
remaining_lifespan =
  max(lifespan_limit - age, 0)

age_ratio =
  age / max(lifespan_limit, 1)

aging_phase =
  lookup_aging_phase(age_ratio, aging_phase_threshold_table)
```

规则：

1. `lifespan_limit` 应大于 0；公式中的 `max(lifespan_limit, 1)` 只用于避免异常数据造成除零。
2. 寿元上限变化后，应重新计算 `remaining_lifespan`、`age_ratio`、`aging_phase` 和 `lifespan_pressure_state`。
3. `age_ratio >= 1.0` 应至少进入 `exhausted` 风险语境，并交给系统层死亡 / 转世或续命检查承接。

### 5.3 体魄与精力最终值

体魄 / 精力最终值拆分为先天最终值和后天最终值，供生机、元气等公式读取。

```text
base_physique_innate_final =
  base_physique.innate_value
  * physique_aging_phase_multiplier

base_physique_acquired_final =
  (
    base_physique.acquired_permanent_delta
    + base_physique.temporary_modifiers_total
  )
  * physique_aging_phase_multiplier

base_physique.effective_value =
  base_physique_innate_final
  + base_physique_acquired_final
```

```text
base_vigor_innate_final =
  base_vigor.innate_value
  * vigor_aging_phase_multiplier

base_vigor_acquired_final =
  (
    base_vigor.acquired_permanent_delta
    + base_vigor.temporary_modifiers_total
  )
  * vigor_aging_phase_multiplier

base_vigor.effective_value =
  base_vigor_innate_final
  + base_vigor_acquired_final
```

规则：

1. `temporary_modifiers_total` 是当前有效临时修正合计；它属于后天来源，但不回写为永久后天属性。
2. `physique_aging_phase_multiplier` 和 `vigor_aging_phase_multiplier` 默认无影响时为 `100%`。
3. 其他公式读取 `base_physique_*_final` 或 `base_vigor_*_final` 时，不再额外乘一次年龄阶段。

### 5.4 非灵根资质有效值

根骨和悟性不读取年龄阶段。

```text
aptitude_bone.effective_value =
  aptitude_bone.innate_value
  + aptitude_bone.acquired_permanent_delta
  + aptitude_bone.temporary_modifiers_total

aptitude_comprehension.effective_value =
  aptitude_comprehension.innate_value
  + aptitude_comprehension.acquired_permanent_delta
  + aptitude_comprehension.temporary_modifiers_total
```

灵根在 MVP 只读取先天结果：

```text
aptitude_root.effective_value =
  aptitude_root.innate_value
```

`root_affinity_tags` 和 `root_affinity_profile` 只读取先天相性结构。外部效果影响灵根相关判定时，应在对应系统中作为状态、资源、道功、法宝、阵法或事件效果参与结算，不写回灵根字段。

### 5.5 生机最大值

生机最大值读取体魄最终值：

```text
bar_vitality_max =
  (
    base_physique_innate_final * vitality_innate_physique_coeff_percent
    + base_physique_acquired_final * vitality_acquired_physique_coeff_percent
    + vitality_flat_bonus
  )
  * vitality_amplifier_percent
```

规则：

1. 常规情况下，`vitality_innate_physique_coeff_percent` 与 `vitality_acquired_physique_coeff_percent` 相同。
2. 分成先天 / 后天系数只是为了后续调参和特殊效果预留。
3. `vitality_flat_bonus` 是生机绝对值加成合计，可以来自后天永久、临时状态、资源效果、伤势修复或事件结果。
4. `vitality_amplifier_percent` 是生机增幅系数合计；多个来源在同一乘区内用加减法汇总。
5. 年龄阶段影响已经体现在体魄最终值中，不在生机公式中再次计算。

### 5.6 元气最大值

元气最大值读取精力最终值：

```text
bar_qi_max =
  (
    base_vigor_innate_final * qi_innate_vigor_coeff_percent
    + base_vigor_acquired_final * qi_acquired_vigor_coeff_percent
    + qi_flat_bonus
  )
  * qi_amplifier_percent
```

规则：

1. 常规情况下，`qi_innate_vigor_coeff_percent` 与 `qi_acquired_vigor_coeff_percent` 相同。
2. 分成先天 / 后天系数只是为了后续调参和特殊效果预留。
3. `qi_flat_bonus` 是元气绝对值加成合计，可以来自后天永久、临时状态、资源效果、功法效果或事件结果。
4. `qi_amplifier_percent` 是元气增幅系数合计；多个来源在同一乘区内用加减法汇总。
5. 年龄阶段影响已经体现在精力最终值中，不在元气公式中再次计算。

### 5.7 神念最大值

神念最大值不读取体魄、精力、年龄阶段或悟性资质，使用独立基础常量：

```text
bar_mind_max =
  (mind_base_value + mind_flat_bonus)
  * mind_fixed_multiplier_percent
```

规则：

1. `mind_base_value` 默认为所有角色相同的常量。
2. `mind_base_value` 用于避免无加成时神念为 0 造成计算异常。神念为 0 在语义上接近角色死亡，因此常态角色不应以 0 作为神念上限基准。
3. `mind_flat_bonus` 是神念绝对值加成合计，可以来自后天永久、临时状态、功法、资源效果或事件结果。
4. `mind_fixed_multiplier_percent` 是神念固定系数乘区；多个来源在同一乘区内用加减法汇总。
5. 神念公式不承接年龄阶段乘区。

### 5.8 保留值与当前值约束

```text
bar_vitality_available_max =
  bar_vitality_max - bar_vitality_reserve

bar_qi_available_max =
  bar_qi_max - bar_qi_reserve

bar_mind_available_max =
  bar_mind_max - bar_mind_reserve
```

约束：

```text
0 <= bar_vitality_reserve <= bar_vitality_max
0 <= bar_qi_reserve <= bar_qi_max
0 <= bar_mind_reserve <= bar_mind_max

0 <= bar_vitality_current <= bar_vitality_available_max
0 <= bar_qi_current <= bar_qi_available_max
0 <= bar_mind_current <= bar_mind_available_max
```

当上限或保留值变化导致当前值超过可支配上限时，当前值应钳制到新的可支配上限，并写入结算日志或状态变更记录。

### 5.9 丹药延寿上限

丹药延寿完成炼化后可以增加寿元上限，但有效计入部分必须满足：

```text
non_pill_lifespan_limit =
  current_lifespan_limit_excluding_pill_sources

pill_lifespan_extension_cap =
  floor(non_pill_lifespan_limit * pill_lifespan_extension_cap_ratio)

effective_pill_lifespan_extension_total =
  min(pill_lifespan_extension_total_raw, pill_lifespan_extension_cap)

lifespan_limit =
  non_pill_lifespan_limit
  + effective_pill_lifespan_extension_total
```

规则：

1. `pill_lifespan_extension_total_raw` 统计丹药来源原始延寿总量。
2. `pill_lifespan_extension_total` 只记录有效计入寿元上限的丹药延寿部分。
3. 超出上限的部分不计入有效 `lifespan_limit`，但必须记录丹药来源、原始延寿值、实际生效值和被上限钳制值。
4. 若后续突破、折寿或其他非丹药来源改变 `non_pill_lifespan_limit`，寿元模块按当前非丹药总寿元重新计算丹药延寿可生效上限。

### 5.10 推荐重算顺序

```text
寿元来源汇总
→ remaining_lifespan / age_ratio / aging_phase / lifespan_pressure_state
→ 体魄 / 精力年龄阶段乘区
→ 体魄 / 精力最终值
→ 根骨 / 悟性有效值
→ 生机 / 元气 / 神念最大值
→ 保留值与当前值钳制
→ 可见摘要、日志和 Debug 缓存
```

该顺序是数值重算建议，不强制实现层使用同名函数或同一缓存结构。

## 6. 配置表或根表

### 6.1 年龄阶段数值表

| 字段 | 含义 | 备注 |
| --- | --- | --- |
| `aging_phase_id` | 年龄阶段 ID | 如 `childhood`、`growth`、`prime`、`aging`、`exhausted` |
| `age_ratio_min` | 阶段起始年龄比例 | 闭区间或半开区间规则待补 |
| `age_ratio_max` | 阶段结束年龄比例 | 需避免阶段重叠 |
| `physique_aging_phase_multiplier` | 体魄年龄阶段乘区 | 默认无影响为 `100%` |
| `vigor_aging_phase_multiplier` | 精力年龄阶段乘区 | 默认无影响为 `100%` |
| `lifespan_pressure_state` | 默认寿元压力状态 | 可被风险标签升级 |

### 6.2 派生状态系数表

| 字段 | 含义 | 备注 |
| --- | --- | --- |
| `vitality_innate_physique_coeff_percent` | 体魄先天最终值到生机上限的系数 | 常规可与后天系数相同 |
| `vitality_acquired_physique_coeff_percent` | 体魄后天最终值到生机上限的系数 | 为特殊调参预留 |
| `qi_innate_vigor_coeff_percent` | 精力先天最终值到元气上限的系数 | 常规可与后天系数相同 |
| `qi_acquired_vigor_coeff_percent` | 精力后天最终值到元气上限的系数 | 为特殊调参预留 |
| `mind_base_value` | 神念基础常量 | 应大于 0 |
| `default_percent` | 默认百分比乘区 | 通常为 `100%` |

### 6.3 派生状态加成汇总表

| 字段 | 含义 | 备注 |
| --- | --- | --- |
| `vitality_flat_bonus` | 生机绝对值加成合计 | 来源包括状态、资源、事件等 |
| `vitality_amplifier_percent` | 生机增幅乘区 | 同乘区加减汇总 |
| `qi_flat_bonus` | 元气绝对值加成合计 | 来源包括状态、资源、功法、事件等 |
| `qi_amplifier_percent` | 元气增幅乘区 | 同乘区加减汇总 |
| `mind_flat_bonus` | 神念绝对值加成合计 | 来源包括状态、资源、功法、事件等 |
| `mind_fixed_multiplier_percent` | 神念固定乘区 | 同乘区加减汇总 |

### 6.4 寿元配置表

| 字段 | 含义 | 备注 |
| --- | --- | --- |
| `pill_lifespan_extension_cap_ratio` | 丹药延寿有效总量上限比例 | 例如 0.2 表示不超过当前非丹药总寿元的 20% |
| `lifespan_pressure_safe_ratio` | 安全压力阈值 | 待补 |
| `lifespan_pressure_pressured_ratio` | 有压力阈值 | 待补 |
| `lifespan_pressure_urgent_ratio` | 紧急阈值 | 待补 |
| `lifespan_pressure_exhausted_ratio` | 耗尽阈值 | 通常承接 `age_ratio >= 1.0` |

## 7. 结算输出

### 7.1 推荐缓存字段

基础属性数值缓存：

```text
BaseAttributeWithAging {
  innate_value: number
  acquired_permanent_delta: number
  temporary_modifiers_total: number
  aging_phase_multiplier: number
  innate_final_value: number
  acquired_final_value: number
  effective_value: number
}
```

非灵根资质缓存：

```text
AptitudeBalanceCache {
  aptitude_bone_effective_value: number
  aptitude_comprehension_effective_value: number
  aptitude_root_effective_value: number
}
```

派生状态数值缓存：

```text
DerivedBarBalanceCache {
  vitality_innate_physique_coeff_percent: number
  vitality_acquired_physique_coeff_percent: number
  vitality_flat_bonus: number
  vitality_amplifier_percent: number

  qi_innate_vigor_coeff_percent: number
  qi_acquired_vigor_coeff_percent: number
  qi_flat_bonus: number
  qi_amplifier_percent: number

  mind_base_value: number
  mind_flat_bonus: number
  mind_fixed_multiplier_percent: number
}
```

寿元数值缓存：

```text
LifespanBalanceCache {
  non_pill_lifespan_limit: number
  pill_lifespan_extension_total_raw: number
  pill_lifespan_extension_cap: number
  effective_pill_lifespan_extension_total: number
  remaining_lifespan: number
  age_ratio: number
  aging_phase: string
  lifespan_pressure_state: string
}
```

### 7.2 可见输出与日志

普通 UI 可读取：

1. 体魄 / 精力当前有效值。
2. 根骨 / 悟性当前有效值。
3. 灵根先天评级和相性摘要。
4. 生机 / 元气 / 神念当前值、上限、可用容量和保留值摘要。
5. 当前年龄、寿元上限、剩余寿元、寿元压力和年龄阶段。

Debug、QA 或回放可读取：

1. 每个公式输入项和输出项。
2. 年龄阶段乘区来源。
3. 绝对值加成来源合计。
4. 百分比乘区来源合计。
5. 保留值造成的可用容量变化。
6. 丹药延寿原始值、生效值和钳制值。
7. 当前值因上限变化被钳制的日志。

## 8. 调参验收口径

1. 体魄 / 精力年龄阶段乘区只能影响体魄 / 精力最终值，不得在生机、元气或其他公式中重复计算。
2. 根骨、悟性、灵根、灵根相性和神念基础值不得读取年龄阶段。
3. 常规百分比增减幅在同一乘区内加减汇总，不应因多个来源独立连乘导致异常膨胀。
4. 生机、元气、神念的保留值不能超过对应上限，当前值不能超过可支配上限。
5. 神念基础上限应大于 0，避免常态角色神念上限为 0。
6. 丹药延寿有效总量不能超过当前非丹药总寿元按比例计算出的上限。
7. 寿元上限变化后，剩余寿元、年龄比例、年龄阶段和寿元压力必须同步刷新。
8. 缓存字段必须能追溯到角色属性、持续状态记录、资源效果、功法效果、事件结果和数值表，不得成为新的事实来源。

## 9. MVP 范围

MVP 应实现：

1. 通用单乘区公式口径。
2. 寿元派生：剩余寿元、年龄比例、年龄阶段和寿元压力。
3. 体魄 / 精力最终值公式及年龄阶段乘区。
4. 根骨 / 悟性有效值公式。
5. MVP 灵根先天有效值读取。
6. 生机 / 元气 / 神念最大值公式。
7. 派生状态保留值和当前值约束。
8. 丹药延寿有效总量上限公式。
9. 推荐缓存字段和结算日志口径。

MVP 不实现：

1. 多层独立乘区叠乘体系。
2. 后天灵根或临时灵根数值公式。
3. 复杂五行循环、相生相克链或多层灵根谱系数值表。
4. 高阶长期曲线验算和完整平衡样例。
5. 由角色属性数值文档替修炼、战斗、事件系统定义完整收益公式。

## 10. 待裁决与待补

| 项目 | 当前口径 | 需要裁决或补充 |
| --- | --- | --- |
| 体魄 / 精力年龄阶段分段阈值 | 使用年龄比例查表 | 需要确定各阶段阈值和区间闭合规则 |
| `physique_aging_phase_multiplier` 曲线 | 独立影响体魄最终值 | 需要确定幼年、壮年、衰老等阶段倍率 |
| `vigor_aging_phase_multiplier` 曲线 | 独立影响精力最终值 | 需要确定阶段倍率是否与体魄一致 |
| 生机、元气、神念系数表 | 已有字段结构 | 需要确定基础系数、默认值和成长范围 |
| 绝对值加成来源表 | 来源按状态 / 资源 / 功法 / 事件汇总 | 需要列出可叠加、互斥和覆盖规则 |
| 百分比增减幅来源细则 | 同乘区加减汇总 | 需要明确负值下限、上限和异常钳制 |
| 寿元压力阈值 | 已有压力状态枚举 | 需要确定 safe / pressured / urgent / exhausted 阈值 |
| 丹药延寿比例 | 按非丹药总寿元比例限制 | 需要确认 MVP 默认比例 |
| 数值验算样例 | 暂未纳入本文 | 需要在 `08_数值验算.md` 中补齐样例和曲线检查 |

## 11. 包内关联文档

| 文档 | 关系 |
| --- | --- |
| `../02_系统设计/02_角色与真灵设计/02_角色属性.md` | 承接：提供字段语义、系统边界、来源 / 持续性口径、寿元职责和 UI 边界 |
| `../02_系统设计/02_角色与真灵设计/07_角色持续状态系统.md` | 承接：读取持续状态导出的临时后天修正、派生状态 tick 结果和叠加边界 |
| `08_数值验算.md` | 验算：后续用于验证年龄曲线、派生状态上限、寿元压力和丹药延寿上限 |
| `02_修炼公式与数值设计.md` | 被承接：修炼公式可读取本文输出的属性有效值、派生状态上限和寿元压力 |
| `03_战斗公式与数值设计.md` | 被承接：战斗公式可读取本文输出的生机、元气、神念和属性有效值 |
| `../04_交互设计/03_角色页面设计.md` | 展示：读取本文的可见摘要和 Debug 边界 |

## 12. 维护记录

2026-05-11：按数值设计模板重组本文，确立本文为角色属性数值层唯一来源；从系统文档迁入寿元派生、保留值约束、非灵根资质有效值和丹药延寿上限口径，并整理包内相对引用。
