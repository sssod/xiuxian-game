# QA_自动检查报告

# QA 自动检查报告（v2.3）

**生成日期**：2026-05-07

**检查对象**：`修仙轮回沙盒_设计文档包_v2.3_calendar_preinput`

---

## 1. 包结构检查

通过：

```
README.md
MANIFEST.md
01_基础与总览/
02_核心系统规格/
03_实现交付/
04_UIUX与界面规范/
```

本包未包含 UI 效果图 prompt 文档。

---

## 2. 当前主字段检查

本包主字段：

```
world_day
world_hour
hour_tick
macro_period_id
TimeConfig
SpeedState
Command
CommandQueue
CommandValidationResult
FallbackState
ManagedActionState
LocalTimeStopState
CatchupSettlementState
FormalEncounterState
GlobalPauseState
TimelineReplay
ActionResourceInputBinding
ActiveResourceEffect
```

---

## 3. 时间规则检查

通过项：

```
N1 = 15 秒 / 游戏小时；
F1 = 2 秒 / 游戏日；
B1 = 60 秒 / 游戏小时；
P0 = 游戏时间不流动；
C1 = 追赶补结算；
gap 上限 = 24 游戏小时；
服务器最小结算单位 = 1 游戏小时。
```

---

## 4. 队列规则检查

通过项：

```
当前执行指令 + 最多 3 条预输入指令；
入队时合法性校验；
执行前再次合法性校验；
自动兜底 force_speed=N1；
自动兜底 f1_eligible=false；
显式托管与自动兜底分离；
突破开启指令与 B1 突破挑战分离。
```

---

## 5. UI 范围检查

通过项：

```
顶部使用世界日历与速度状态；
底部使用行动队列面板；
速度显示使用中性原因；
局部时停只向事件玩家显示 gap；
B1 使用专用交锋轮界面；
Figma 当前主界面稿未作为本包版面依据。
```

---

## 6. 后续人工检查建议

1. 实现前检查模板数据是否补齐 `event_time_mode`、节点 `risk_level`、行动 `f1_eligible_when_explicit`。
2. UI 原型阶段检查私人归因字段是否只出现在 Debug 面板。
3. 存档回放阶段检查 `TimelineReplay.final_state_hash` 是否可用于回归测试。