# 文档替换清单（v0.1）

> 用于把本修订包合并回 Library 中 v2.2 文档包。

---

## 1. 必须整段替换

| Library v2.2 文件 | 替换内容 |
|---|---|
| `02_核心系统规格/01_房间回合推进_权威结算_v2.2.md` | 用 `01_修订文档/02_房间与时间推进_替换稿_v0.1.md` 替换时间推进、阶段机、锁定、结算、插入事件、回放章节 |
| `02_核心系统规格/03_个人行动经济_移动通行_托管_v2.2.md` | 用 `01_修订文档/03_个人行动队列与指令预输入_替换稿_v0.1.md` 替换行动规划、指令队列、托管、兜底、大突破固定行动章节 |
| `02_核心系统规格/08_事件突破战斗_v2.2.md` | 用 `01_修订文档/04_事件突破战斗时间规则_替换稿_v0.1.md` 替换事件时间、交锋、突破章节 |
| `03_实现交付/01_运行时状态_数据模型_结果包_v2.2.md` | 用 `01_修订文档/05_运行时状态与结果包字段修订_v0.1.md` 同步字段 |
| `04_UIUX与效果图/01_UIUX需求方案_v2.2.md` | 用 `01_修订文档/08_UIUX时间速度与队列界面修订_v0.1.md` 替换顶部阶段条、底部预算条、锁定提交和托管 UI 章节 |

---

## 2. 必须删除的旧句式

```text
当前默认每回合 5 个游戏天
玩家最小推进单位统一称为回合
回合时长可配置，当前默认 5 个游戏天
决策阶段倒计时可配置，当前默认 2 分钟
全员锁定或超时后自动结算
结算阶段不能主动暂停，只能被插入事件链挂起
个人行动指令预输入阶段
锁定 / 超时等待
服务端统一结算阶段
天级回合时间预算条
大境界突破属于固定执行时间持续行动
关键突破物开始时消耗
无后续队列时角色自动进入休整待机
休整待机属于 F1 长期低交互行动
低价值事件默认自动处理
```

---

## 3. 必须新增的关键词

```text
自适应流速
共享世界日历
F1 高速推进
N1 常态推进
B1 交锋慢速
P0 全局暂停
C1 追赶补结算
个人局部时停
gap 不超过 1 游戏日
当前执行指令 + 最多 3 条预输入指令
执行前再次合法性校验
自动兜底强制 N1
自动兜底不具备 F1 资格
显式托管行动
闭关三档预案
正式交锋轮
突破挑战 B1
```

---

## 4. 推荐下一版本号

若合并为全量新包，建议命名：

```text
修仙轮回沙盒_设计文档包_v2.3_calendar_preinput
```

如果仍保留 v2.2 主包，只作为补丁，则建议命名：

```text
v2.2_time_preinput_patch_v0.1
```
